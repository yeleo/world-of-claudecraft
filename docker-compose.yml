services:
  postgres:
    image: postgres:16-alpine
    container_name: eastbrook-db
    restart: unless-stopped
    environment:
      POSTGRES_USER: eastbrook
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?Set POSTGRES_PASSWORD in .env. See .env.example.}
      POSTGRES_DB: eastbrook
    ports:
      - "127.0.0.1:5433:5432"
    volumes:
      - eastbrook_pgdata:/var/lib/postgresql/data
    # pg_isready is a trivial ping, so polling it often costs nothing, but a permanent
    # 1Hz steady-state probe is unwanted overhead on the production stack this file also
    # deploys (see DEPLOY.md). start_period + start_interval give the fast 1s cadence
    # ONLY during a cold boot (failures inside start_period don't spend the retry
    # budget at all), while interval/timeout/retries return to the same steady-state
    # values used before this change, keeping the ~50s outer ceiling for a genuinely
    # cold initdb intact. -h pins the probe to the TCP listener so it cannot read
    # healthy off initdb's transient unix-socket-only bootstrap server.
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -h 127.0.0.1 -U eastbrook -d eastbrook"]
      interval: 5s
      timeout: 3s
      retries: 10
      start_period: 60s
      start_interval: 1s

  # The full game server (client + API + world). `docker compose up -d`
  # then open http://localhost:8787 — or put a TLS reverse proxy in front
  # for remote hosting (see README).
  game:
    image: eastbrook-game:${EASTBROOK_IMAGE_TAG:-local}
    build:
      context: .
      args:
        # Build-time public client config inlined into the bundle (see .env.example).
        VITE_TURNSTILE_SITEKEY: ${VITE_TURNSTILE_SITEKEY:-}
        VITE_REOWN_PROJECT_ID: ${VITE_REOWN_PROJECT_ID:-}
        VITE_WALLET_DISABLED: ${VITE_WALLET_DISABLED:-1}
        VITE_DISCORD_DISABLED: ${VITE_DISCORD_DISABLED:-1}
    container_name: eastbrook-game
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      DATABASE_URL: postgres://eastbrook:${POSTGRES_PASSWORD:?Set POSTGRES_PASSWORD in .env. See .env.example.}@postgres:5432/eastbrook
      PORT: "8787"
      # Runtime: node flags for the game process, chiefly the V8 heap ceiling
      # (--max-old-space-size). Node sizes its default old-space from TOTAL SYSTEM
      # memory, not from the container's cgroup limit, so on a box larger than
      # mem_limit an unset heap flag lets V8 grow past the limit and get OOM-killed
      # mid-tick by the kernel. Sizing the heap UNDER mem_limit means an overrun
      # kills the container (which docker then restarts) instead of the host OOM
      # killer wedging the box. Empty (unset) keeps node's own defaults: the
      # recommended production value lives in .env.example, never baked in here.
      NODE_OPTIONS: ${NODE_OPTIONS:-}
      # Runtime: the realm player admission cap. A fresh WebSocket join is refused
      # once the realm holds this many sessions (server/ws_auth.ts); /api/status
      # advertises the value so the client realm list can show Full honestly. Empty
      # (unset) yields the built-in default of 5000; an explicit 0 disables the cap.
      MAX_PLAYERS_PER_REALM: ${MAX_PLAYERS_PER_REALM:-}
      # Runtime: Postgres clients this process keeps in its pool (server/db.ts).
      # Empty (unset) yields the built-in default of 10; accepted range is 1 to
      # 97 and anything else falls back to the default with an error at boot.
      DB_POOL_MAX_CLIENTS: ${DB_POOL_MAX_CLIENTS:-}
      # Runtime: the Rift Forge kill switch (server/rift_forge_gate.ts). The
      # forge wire is open by default; 0 (or false/off/no) pauses it on this
      # realm. Forwarded so the switch actually reaches the server: a variable
      # set only in the host .env never does unless it is listed here.
      RIFT_FORGE_ENABLED: ${RIFT_FORGE_ENABLED:-}
      # Runtime: storage price override (server/storage_prices.ts), one JSON object
      # of copper price lists, e.g. {"bankSockets":[4 ints]} (bankExpansions and
      # vaultUpgrades too), kept on ONE line in a .env file. Empty (unset) keeps the
      # compiled defaults; a rejected dimension falls back by itself and is reported
      # on the console at boot.
      STORAGE_PRICES: ${STORAGE_PRICES:-}
      # Runtime: database-wide lifetime insert ceiling for the keep-forever
      # bank_ledger (server/bank_ledger_growth_budget.ts). Empty (unset) yields
      # the built-in default of 10,000,000 rows. Every process sharing
      # DATABASE_URL must use the same value as the stored singleton or boot
      # fails; raising it is a maintenance-window operation (DEPLOY.md).
      BANK_LEDGER_GROWTH_HARD_LIMIT_ROWS: ${BANK_LEDGER_GROWTH_HARD_LIMIT_ROWS:-}
      # Community test realms can opt in to a ready-made level-20 roster for
      # every newly created account. The production-safe default is disabled.
      PROVISION_TEST_ACCOUNTS: ${PROVISION_TEST_ACCOUNTS:-0}
      # PBE only: boost registrations to a BiS roster and top existing
      # characters up to the current BiS kit at world join (server/pbe_boost.ts).
      # NEVER set in production. The production-safe default is disabled.
      PBE_BOOST_ACCOUNTS: ${PBE_BOOST_ACCOUNTS:-}
      # Target the game server 302-redirects /wiki to when hit directly (not via the
      # reverse proxy). Public origin in prod; localhost fallback for local dev.
      WIKI_URL: ${WIKI_URL:-/wiki/}
      # Runtime public origin for server-generated absolute URLs such as player-card
      # Open Graph images. Set per environment, for example dev vs production.
      PUBLIC_ORIGIN: ${PUBLIC_ORIGIN:-}
      USERNAME_BANLIST: ${USERNAME_BANLIST:-}
      USERNAME_BANLIST_FILE: ${USERNAME_BANLIST_FILE:-}
      CHAT_CENSOR_LIST: ${CHAT_CENSOR_LIST:-}
      CHAT_CENSOR_FILE: ${CHAT_CENSOR_FILE:-/app/data/censor/censor_soft.txt}
      CHAT_FILTER_HARD_FILE: ${CHAT_FILTER_HARD_FILE:-/app/data/censor/censor_hard.txt}
      # Runtime: server-side Turnstile verification (see .env.example). Empty → gate off.
      TURNSTILE_SECRET: ${TURNSTILE_SECRET:-}
      # Runtime: native app attestation for iOS DeviceCheck and Google Play Integrity.
      # Secrets are read from .env on the host and passed through to the container.
      NATIVE_ATTESTATION_REQUIRED: ${NATIVE_ATTESTATION_REQUIRED:-}
      GOOGLE_PLAY_INTEGRITY_SERVICE_ACCOUNT_JSON: ${GOOGLE_PLAY_INTEGRITY_SERVICE_ACCOUNT_JSON:-}
      GOOGLE_PLAY_INTEGRITY_SIGNING_PEM: ${GOOGLE_PLAY_INTEGRITY_SIGNING_PEM:-}
      GOOGLE_PLAY_INTEGRITY_PACKAGE_NAME: ${GOOGLE_PLAY_INTEGRITY_PACKAGE_NAME:-com.worldofclaudecraft}
      GOOGLE_PLAY_INTEGRITY_CERT_DIGESTS: ${GOOGLE_PLAY_INTEGRITY_CERT_DIGESTS:-}
      GOOGLE_PLAY_INTEGRITY_DEVICE_VERDICT: ${GOOGLE_PLAY_INTEGRITY_DEVICE_VERDICT:-MEETS_DEVICE_INTEGRITY}
      # Solana Store (Seeker) artifact allowlist: fail-closed while unset, so the
      # Solana native-auth arm and Seeker claim/spin stay off until configured.
      SEEKER_SOLANA_INTEGRITY_PACKAGE_NAME: ${SEEKER_SOLANA_INTEGRITY_PACKAGE_NAME:-}
      SEEKER_SOLANA_INTEGRITY_CERT_DIGESTS: ${SEEKER_SOLANA_INTEGRITY_CERT_DIGESTS:-}
      SEEKER_SOLANA_INTEGRITY_DEVICE_VERDICT: ${SEEKER_SOLANA_INTEGRITY_DEVICE_VERDICT:-MEETS_DEVICE_INTEGRITY}
      APPLE_TEAM_ID: ${APPLE_TEAM_ID:-}
      APPLE_BUNDLE_ID: ${APPLE_BUNDLE_ID:-com.worldofclaudecraft}
      APPLE_DEVICECHECK_KEY_ID: ${APPLE_DEVICECHECK_KEY_ID:-}
      APPLE_DEVICECHECK_SIGNING_PEM: ${APPLE_DEVICECHECK_SIGNING_PEM:-}
      APPLE_DEVICECHECK_ENV: ${APPLE_DEVICECHECK_ENV:-production}
      # Runtime: opt-in world-loop perf diagnostics (PERF_TICK_LOG=1): the 20 Hz
      # heartbeat log line (tickHz, per-phase p95/max) plus over-budget stutter
      # traces. Off by default so production logs stay quiet.
      PERF_TICK_LOG: ${PERF_TICK_LOG:-}
      # Runtime: client perf beacon retention in days for the nightly sweep
      # (server/http/config.ts, code default 14). The host .env sets a longer
      # window (90) so device/FPS history spans release cycles; without this
      # passthrough the container silently runs the code default and discards
      # the longitudinal data the perf investigations rely on.
      PERF_REPORT_RETENTION_DAYS: ${PERF_REPORT_RETENTION_DAYS:-}
      # Runtime: the sibling retention knobs config.ts reads that were missing
      # the same passthrough, so a host .env override silently fell back to the
      # code defaults exactly like PERF_REPORT_RETENTION_DAYS did. Empty is
      # safe: numberOr treats the empty string as absent.
      CHAT_LOG_RETENTION_DAYS: ${CHAT_LOG_RETENTION_DAYS:-}
      UNSTUCK_REPORT_RETENTION_DAYS: ${UNSTUCK_REPORT_RETENTION_DAYS:-}
      DAILY_REWARD_EVENTS_RETENTION_DAYS: ${DAILY_REWARD_EVENTS_RETENTION_DAYS:-}
      ONLINE_SAMPLES_RETENTION_DAYS: ${ONLINE_SAMPLES_RETENTION_DAYS:-}
      # Runtime: the published OTA bundle manifest POST /api/ota/updates reads
      # (server/ota_updates.ts). Empty (unset) keeps OTA dark: the endpoint
      # answers "no update" and the native apps keep their current bundle. Must
      # be https, and must share its origin with the bundle URLs inside it.
      OTA_MANIFEST_URL: ${OTA_MANIFEST_URL:-}
      OTA_DELTA_DISABLED: ${OTA_DELTA_DISABLED:-}
      # Runtime: bearer token that gates GET /metrics (server/http/health.ts).
      # Empty (unset) leaves the endpoint feature-off (404); a non-empty value
      # turns it on behind Authorization: Bearer <token>. Set on the host .env by
      # the monitoring deploy so Prometheus can scrape the RED + process metrics.
      METRICS_TOKEN: ${METRICS_TOKEN:-}
      # Runtime: the ONLY gate on the deploy countdown endpoint (nothing
      # restricts /internal/* to loopback), so treat it as a real secret.
      RESTART_COUNTDOWN_SECRET: ${RESTART_COUNTDOWN_SECRET:-}
      # Runtime: Discord OAuth + the secret-gated game <-> Discord bot channel.
      DISCORD_CLIENT_ID: ${DISCORD_CLIENT_ID:-}
      DISCORD_CLIENT_SECRET: ${DISCORD_CLIENT_SECRET:-}
      DISCORD_GUILD_ID: ${DISCORD_GUILD_ID:-}
      DISCORD_GUILD_INVITE: ${DISCORD_GUILD_INVITE:-}
      DISCORD_BOT_SECRET: ${DISCORD_BOT_SECRET:-}
      # Runtime: the game server uses the bot token to auto-join a linking player to
      # the official guild (guilds.join). Empty disables auto-join (verify-only).
      DISCORD_BOT_TOKEN: ${DISCORD_BOT_TOKEN:-}
      # Runtime: the /api/discord status cache levers (server/discord_status_cache.ts;
      # operator rows in DEPLOY.md). Empty, non-numeric, or non-positive reads as the
      # in-code default (15000 ms TTL, 2000 entries), so an unset key is always safe.
      DISCORD_STATUS_CACHE_TTL_MS: ${DISCORD_STATUS_CACHE_TTL_MS:-}
      DISCORD_STATUS_CACHE_MAX_ENTRIES: ${DISCORD_STATUS_CACHE_MAX_ENTRIES:-}
      # Runtime: GitHub OAuth for the in-game developer badge. Both id+secret must be
      # set to enable "Link GitHub"; empty hides it exactly like Discord. GITHUB_REPO
      # selects which repo's contributor stats back the badge (code default:
      # levy-street/world-of-claudecraft); GITHUB_TOKEN lifts the GitHub API rate limit
      # for the contributor + News/Releases fetches. See .env.example.
      GITHUB_OAUTH_CLIENT_ID: ${GITHUB_OAUTH_CLIENT_ID:-}
      GITHUB_OAUTH_CLIENT_SECRET: ${GITHUB_OAUTH_CLIENT_SECRET:-}
      GITHUB_REPO: ${GITHUB_REPO:-}
      GITHUB_TOKEN: ${GITHUB_TOKEN:-}
      WOC_DAILY_REWARD_SERVICE_URL: ${WOC_DAILY_REWARD_SERVICE_URL:-}
      WOC_DAILY_REWARD_SERVICE_SECRET: ${WOC_DAILY_REWARD_SERVICE_SECRET:-}
      WOC_DAILY_REWARD_CONFIG_TTL_MS: ${WOC_DAILY_REWARD_CONFIG_TTL_MS:-}
      WOC_ECONOMY_SERVICE_URL: ${WOC_ECONOMY_SERVICE_URL:-}
      WOC_ECONOMY_INTERNAL_SECRET: ${WOC_ECONOMY_INTERNAL_SECRET:-}
      # The $WOC Exchange. Ships disabled: unset leaves every path answering
      # woc_market.disabled and the sweep never starts. WOC_MARKET_SERVICE_URL
      # points at the economy service's /v1/market/ prefix, authenticated with
      # the WOC_ECONOMY_INTERNAL_SECRET above (the market proxy shares it).
      # The DEV arms are deliberately absent here: they are dev-only and this
      # compose is the production deployment.
      WOC_MARKET_ENABLED: ${WOC_MARKET_ENABLED:-}
      WOC_MARKET_SERVICE_URL: ${WOC_MARKET_SERVICE_URL:-}
      WOC_MARKET_EXCLUDED_ITEM_IDS: ${WOC_MARKET_EXCLUDED_ITEM_IDS:-}
      WOC_MARKET_LISTINGS_RETENTION_DAYS: ${WOC_MARKET_LISTINGS_RETENTION_DAYS:-}
      WOC_MARKET_ABANDONS_RETENTION_DAYS: ${WOC_MARKET_ABANDONS_RETENTION_DAYS:-}
      WOC_MARKET_OFFERS_RETENTION_DAYS: ${WOC_MARKET_OFFERS_RETENTION_DAYS:-}
      WOC_MARKET_CUSTODY_CLAIMS_RETENTION_DAYS: ${WOC_MARKET_CUSTODY_CLAIMS_RETENTION_DAYS:-}
      WOC_MARKET_CONFIRMING_REVIEW_HOURS: ${WOC_MARKET_CONFIRMING_REVIEW_HOURS:-}
      # WOC_MARKET_DEV_SERVICE / WOC_MARKET_DEV_PRICE_MICRO_USD are deliberately
      # NOT passed through: compose is the production deployment and the dev
      # economy must be unreachable from it (the ALLOW_DEV_COMMANDS posture).
      SOLANA_RPC_URL: ${SOLANA_RPC_URL:-}
      WOC_MINT: ${WOC_MINT:-}
      META_CAPI_PIXEL_ID: ${META_CAPI_PIXEL_ID:-}
      META_CAPI_ACCESS_TOKEN: ${META_CAPI_ACCESS_TOKEN:-}
      META_CAPI_TEST_EVENT_CODE: ${META_CAPI_TEST_EVENT_CODE:-}
      META_CAPI_API_VERSION: ${META_CAPI_API_VERSION:-}
      # Runtime: outbound email (server/email/sender.ts). All empty → console
      # transport (mail is logged, not sent). EMAIL_PROVIDER=ses selects AWS SES
      # (needs EMAIL_SES_REGION or AWS_REGION plus EMAIL_FROM; credentials via the
      # SDK default chain: IAM instance role, or the AWS_* keys below). Otherwise
      # EMAIL_API_URL+EMAIL_API_KEY+EMAIL_FROM selects the generic HTTP transport.
      EMAIL_PROVIDER: ${EMAIL_PROVIDER:-}
      EMAIL_FROM: ${EMAIL_FROM:-}
      EMAIL_BASE_URL: ${EMAIL_BASE_URL:-}
      EMAIL_SES_REGION: ${EMAIL_SES_REGION:-}
      AWS_REGION: ${AWS_REGION:-}
      AWS_ACCESS_KEY_ID: ${AWS_ACCESS_KEY_ID:-}
      AWS_SECRET_ACCESS_KEY: ${AWS_SECRET_ACCESS_KEY:-}
      EMAIL_API_URL: ${EMAIL_API_URL:-}
      EMAIL_API_KEY: ${EMAIL_API_KEY:-}
      # Optional Steam account link and Book of Deeds achievement mirror.
      STEAM_ENABLED: ${STEAM_ENABLED:-}
      STEAM_APP_ID: ${STEAM_APP_ID:-}
      STEAM_WEB_API_KEY: ${STEAM_WEB_API_KEY:-}
      # Optional Epic account link and Book of Deeds achievement mirror.
      # Dark unless EPIC_ENABLED is exactly 1 (see DEPLOY.md). Secrets stay on
      # the host .env; never commit real values.
      EPIC_ENABLED: ${EPIC_ENABLED:-}
      EPIC_PRODUCT_ID: ${EPIC_PRODUCT_ID:-}
      EPIC_SANDBOX_ID: ${EPIC_SANDBOX_ID:-}
      EPIC_DEPLOYMENT_ID: ${EPIC_DEPLOYMENT_ID:-}
      EPIC_CLIENT_ID: ${EPIC_CLIENT_ID:-}
      EPIC_CLIENT_SECRET: ${EPIC_CLIENT_SECRET:-}
      # Optional deployable SFX packs live outside the image. Export from SFX
      # Studio, run its installer against the host overlay root, and this server
      # reads only the stable runtime manifest plus immutable audio blobs here.
      SFX_PACK_DIR: /app/sfx-runtime/audio/sfx
      # Combat parse capture (server/parse/): inert unless PARSE_CAPTURE=1 and
      # an ingest URL is set. The spool dir must live on the mounted volume
      # below so batches survive a container restart.
      PARSE_CAPTURE: ${PARSE_CAPTURE:-}
      PARSE_INGEST_URL: ${PARSE_INGEST_URL:-}
      PARSE_INGEST_TOKEN: ${PARSE_INGEST_TOKEN:-}
      PARSE_CAPTURE_SURFACES: ${PARSE_CAPTURE_SURFACES:-}
      PARSE_SPOOL_DIR: ${PARSE_SPOOL_DIR:-/app/parse-spool}
      PARSE_SPOOL_MAX_MB: ${PARSE_SPOOL_MAX_MB:-}
      PARSE_ENV_LABEL: ${PARSE_ENV_LABEL:-}
      PARSE_CENSUS: ${PARSE_CENSUS:-}
      PARSE_CENSUS_HOUR: ${PARSE_CENSUS_HOUR:-}
      WOC_BUILD_VERSION: ${WOC_BUILD_VERSION:-}
    # loopback-only: the TLS reverse proxy (Caddy) is the public entrance
    ports:
      - "127.0.0.1:8787:8787"
    volumes:
      - ${EASTBROOK_MEDIA_DIR:-./media-cache}:/app/dist/media
      - ${EASTBROOK_SFX_DIR:-./sfx-runtime}:/app/sfx-runtime:ro
      - ${PARSE_SPOOL_HOST_DIR:-./parse-spool}:/app/parse-spool
      - ${CENSOR_DIR:-./data/censor}:/app/data/censor:ro
    extra_hosts:
      - "host.docker.internal:host-gateway"
    # Exec form with a node one-liner, deliberately unlike the CMD-SHELL healthcheck
    # the postgres service uses above: the runtime image is a Debian slim base
    # (node:26-slim) and has no curl or wget, but it does ship node. The probe hits
    # /livez and nothing else. /livez reports a WEDGED game loop, not merely a live
    # process, so it catches the failure
    # `restart: unless-stopped` cannot see (that policy only acts on process exit), and
    # it deliberately stays 200 while the server drains, so a graceful shutdown is never
    # misread as a wedge. /readyz flips to 503 while draining and would do exactly that.
    # The probe must exit nonzero on BOTH a non-200 status and a refused connection,
    # hence the explicit statusCode check and the 'error' handler: a bare http.get exits
    # 0 on a 503 and throws on a refusal. start_period covers a cold boot (the DB retry
    # loop, the schema sweep, the cache warms); failures inside it do not spend retries.
    healthcheck:
      test: ["CMD", "node", "-e", "require('http').get('http://127.0.0.1:8787/livez',(r)=>process.exit(r.statusCode===200?0:1)).on('error',()=>process.exit(1))"]
      interval: 15s
      timeout: 5s
      retries: 4
      start_period: 60s
    # Time docker allows between SIGTERM and SIGKILL. Covers the realistic shutdown
    # path: the character/market/mail saves, ending play sessions, the bank-ledger and
    # deed-record drains, the Steam mirror's 5s deadline, releasing character leases and
    # closing the pool, with headroom over the runbook's `docker compose stop -t 60
    # game` fallback. It is not a strict bound: an adversarial worst case (a character
    # save riding the 65s driver backstop, plus the deadline-free FIFO drains) can still
    # run past it and be killed.
    stop_grace_period: 75s
    # Container memory ceiling. The default suits a host with about 8 GiB of RAM: 5 GiB
    # here holds the recommended 4096 MiB V8 heap (NODE_OPTIONS above) plus native and
    # buffer overhead, and still leaves room for the database container and the OS.
    # Resize it to the host you are running on. Without a ceiling a runaway game process
    # can starve the database and the OS and take the whole host down rather than just
    # itself. mem_limit is the direct runtime Compose Spec key
    # and is honored unconditionally by `docker compose up`; prefer it over the
    # swarm-shaped deploy.resources.limits.memory block, which Compose v2 does honor for
    # `up` but which is the swarm surface (and was a silent no-op under compose v1).
    mem_limit: 5g
    # Pin swap to the memory limit so the container cannot lean on host swap: docker
    # defaults total memory+swap to twice mem_limit when this is unset, so on a host with
    # swap a leaking process would thrash up to 5g of swap before the OOM kill, which is
    # slower and messier than the clean in-container kill the limit exists to produce.
    memswap_limit: 5g

  discord-bot:
    image: eastbrook-game:${EASTBROOK_IMAGE_TAG:-local}
    container_name: eastbrook-discord-bot
    profiles:
      - discord
    restart: unless-stopped
    depends_on:
      game:
        condition: service_started
    environment:
      GAME_SERVER_URL: http://game:8787
      PUBLIC_GAME_URL: ${PUBLIC_GAME_URL:-http://localhost:8787}
      DISCORD_BOT_TOKEN: ${DISCORD_BOT_TOKEN:-}
      DISCORD_CLIENT_ID: ${DISCORD_CLIENT_ID:-}
      DISCORD_GUILD_ID: ${DISCORD_GUILD_ID:-}
      DISCORD_BOT_SECRET: ${DISCORD_BOT_SECRET:-}
      DISCORD_VOICE_CHANNEL_ID: ${DISCORD_VOICE_CHANNEL_ID:-}
      DISCORD_WELCOME_CHANNEL_ID: ${DISCORD_WELCOME_CHANNEL_ID:-}
      DISCORD_TEST_CHANNEL_ID: ${DISCORD_TEST_CHANNEL_ID:-}
      DISCORD_RELAY_CHANNEL_ID: ${DISCORD_RELAY_CHANNEL_ID:-}
      DISCORD_ACTIVITY_CHANNEL_ID: ${DISCORD_ACTIVITY_CHANNEL_ID:-}
      DISCORD_DAILY_REWARDS_CHANNEL_ID: ${DISCORD_DAILY_REWARDS_CHANNEL_ID:-}
      DISCORD_SYNC_NICKNAMES: ${DISCORD_SYNC_NICKNAMES:-1}
      # Runtime: every tuning knob the bot reads, so an operator can turn the
      # incident levers DEPLOY.md documents (the request ceiling, the ban pause,
      # the breaker limit, the forbidden-channel TTL, and the sweep and outbox
      # cadences) without editing and rebuilding the image. The bot code already
      # read all of these; compose forwarded none of them, so on the host every
      # documented lever was inert and the only way to change one was a rebuild.
      # Empty when unset is safe by design: the bot's positiveNumberFromEnv
      # rejects '' and falls back to its built-in default, exactly as if the key
      # were absent. Repeating the code defaults as compose defaults was rejected
      # for the opposite reason: it would put the same numbers in two places and
      # let them drift with nothing to notice.
      DISCORD_MAX_RPS: ${DISCORD_MAX_RPS:-}
      DISCORD_BAN_PAUSE_MS: ${DISCORD_BAN_PAUSE_MS:-}
      DISCORD_BREAKER_LIMIT: ${DISCORD_BREAKER_LIMIT:-}
      DISCORD_FORBIDDEN_TTL_MS: ${DISCORD_FORBIDDEN_TTL_MS:-}
      DISCORD_ROLE_SYNC_INTERVAL_MS: ${DISCORD_ROLE_SYNC_INTERVAL_MS:-}
      DISCORD_PRESENCE_DEBOUNCE_MS: ${DISCORD_PRESENCE_DEBOUNCE_MS:-}
      DISCORD_SWEEP_SLICE_MS: ${DISCORD_SWEEP_SLICE_MS:-}
      DISCORD_SWEEP_SLICE_SIZE: ${DISCORD_SWEEP_SLICE_SIZE:-}
      DISCORD_OUTBOX_POLL_MS: ${DISCORD_OUTBOX_POLL_MS:-}
      DISCORD_OUTBOX_IDLE_MS: ${DISCORD_OUTBOX_IDLE_MS:-}
      DISCORD_OUTBOX_TIMEOUT_MS: ${DISCORD_OUTBOX_TIMEOUT_MS:-}
      # Runtime: the liveness heartbeat. The first two are read by the bot (where
      # it stamps the file, and how often); the third is read ONLY by the
      # healthcheck one-liner below, which decides how old a stamp may be before
      # the container is called unhealthy. Keeping the staleness knob out of the
      # bot is deliberate: the writer must not be able to widen the window the
      # prober judges it by.
      DISCORD_HEARTBEAT_FILE: ${DISCORD_HEARTBEAT_FILE:-}
      DISCORD_HEARTBEAT_INTERVAL_MS: ${DISCORD_HEARTBEAT_INTERVAL_MS:-}
      DISCORD_HEARTBEAT_STALE_MS: ${DISCORD_HEARTBEAT_STALE_MS:-}
    command: ["node", "dist-bot/bot.cjs"]
    # Exec form with a node one-liner for the same reason the game service uses one
    # (node:26-slim ships neither curl nor wget), but it cannot reuse the game's
    # probe shape: the bot serves no HTTP port, so there is no /livez to hit. It
    # tests the FRESHNESS of the heartbeat file instead. Freshness, not existence:
    # a file that exists proves the process started once, while an mtime inside the
    # window proves the scheduler loop is still turning, which is the failure that
    # actually happens (a live process whose loops have stalled). Missing,
    # un-stat-able (a denied path), and stale all exit nonzero, hence the try/catch
    # around statSync. A chmod-000 heartbeat FILE still stats fine (stat needs no
    # read permission) and reads healthy while fresh, which is correct: mtime is the
    # only evidence the probe needs, and a file the bot cannot WRITE stops advancing
    # and goes stale within one window anyway.
    # The s>0?s:90000 guard is deliberate: compose passes '' for an unset host key
    # and Number('') is 0, a garbage value is NaN, and a negative would make the
    # comparison unsatisfiable (permanently unhealthy); none of the three is > 0,
    # so all fall back to the default, matching the bot's own positiveNumberFromEnv.
    # The path is trimmed because bot/config.ts trims it: without the matching trim
    # a padded override would send the write and the stat to two different names.
    # start_period covers the first jittered write, which can land up to about 33s
    # after boot. Note what this does NOT do: a red healthcheck never restarts the
    # container, because `restart: unless-stopped` acts on process exit only. It is
    # the operator signal DEPLOY.md documents, and the reason the probe is worth
    # having even so.
    healthcheck:
      test: ["CMD", "node", "-e", "const fs=require('fs');const p=(process.env.DISCORD_HEARTBEAT_FILE||'').trim()||'/tmp/discord-bot-heartbeat';const s=Number(process.env.DISCORD_HEARTBEAT_STALE_MS);const stale=s>0?s:90000;try{process.exit(Date.now()-fs.statSync(p).mtimeMs<stale?0:1)}catch{process.exit(1)}"]
      interval: 15s
      timeout: 5s
      retries: 4
      start_period: 60s
    # Time docker allows between SIGTERM and SIGKILL. Far shorter than the game's 75s
    # because the bot has no save or drain obligations: nothing durable lives in this
    # process, and it installs no SIGTERM handler, so node's default disposition ends
    # it immediately and the grace window is rarely consumed at all. Any positive
    # value is therefore enough; safety comes from the outbox living server-side and
    # redelivering anything unacked on the next poll, not from covering an in-flight
    # call (a Discord call can ride DISCORD_CALL_TIMEOUT_MS for 15s on its own).
    stop_grace_period: 15s
    # Container memory ceiling. A relay bot's working set is tens of MiB, so 512m is
    # generous headroom rather than a tight fit; the ceiling exists for the neighbours,
    # not for this process. Without one, a leak in the bot can grow until it starves the
    # game and the database sharing the host, turning a cosmetic bot bug into a realm
    # outage. With one, the bot is the only thing that dies.
    mem_limit: 512m
    # Pin swap to the memory limit. Left unset, docker allows total memory plus swap up
    # to twice mem_limit, so on a host with swap a leaking bot would thrash half a gig of
    # host swap (slowing every other container on the box) before anything killed it.
    # Equal values mean it takes a clean in-container OOM kill instead.
    memswap_limit: 512m

  mediawiki-db:
    image: mariadb:11
    container_name: eastbrook-wiki-db
    profiles:
      - wiki
    restart: unless-stopped
    environment:
      MYSQL_DATABASE: ${MEDIAWIKI_DB_NAME:-mediawiki}
      MYSQL_USER: ${MEDIAWIKI_DB_USER:-mediawiki}
      MYSQL_PASSWORD: ${MEDIAWIKI_DB_PASSWORD:-mediawiki-change-me}
      MYSQL_RANDOM_ROOT_PASSWORD: "yes"
    volumes:
      - eastbrook_wikidb:/var/lib/mysql
    healthcheck:
      test: ["CMD-SHELL", "mariadb-admin ping -h 127.0.0.1 -u${MEDIAWIKI_DB_USER:-mediawiki} -p${MEDIAWIKI_DB_PASSWORD:-mediawiki-change-me} --silent"]
      interval: 5s
      timeout: 3s
      retries: 20

  mediawiki:
    build:
      context: .
      dockerfile: mediawiki/Dockerfile
    container_name: eastbrook-wiki
    profiles:
      - wiki
    restart: unless-stopped
    depends_on:
      mediawiki-db:
        condition: service_healthy
    environment:
      MEDIAWIKI_DB_HOST: mediawiki-db
      MEDIAWIKI_DB_NAME: ${MEDIAWIKI_DB_NAME:-mediawiki}
      MEDIAWIKI_DB_USER: ${MEDIAWIKI_DB_USER:-mediawiki}
      MEDIAWIKI_DB_PASSWORD: ${MEDIAWIKI_DB_PASSWORD:-mediawiki-change-me}
      MEDIAWIKI_ADMIN_USER: ${MEDIAWIKI_ADMIN_USER:-WikiAdmin}
      MEDIAWIKI_ADMIN_PASS: ${MEDIAWIKI_ADMIN_PASS:-change-me-admin-password}
      MEDIAWIKI_SECRET_KEY: ${MEDIAWIKI_SECRET_KEY:-local-dev-change-me-world-of-claudecraft}
      MEDIAWIKI_UPGRADE_KEY: ${MEDIAWIKI_UPGRADE_KEY:-local-dev-upgrade-key}
      MEDIAWIKI_SERVER: ${MEDIAWIKI_SERVER:-http://localhost:8080}
    ports:
      - "127.0.0.1:8080:80"
    volumes:
      - eastbrook_wiki_images:/var/www/html/images

volumes:
  eastbrook_pgdata:
  eastbrook_wikidb:
  eastbrook_wiki_images:
