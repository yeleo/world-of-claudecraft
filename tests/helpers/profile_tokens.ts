// The two spellings a static-preset profile or governor read would arrive
// through, as import specifiers (a module reaching for the profile does it by
// importing it). The governor tokens name the REAL module and class
// (src/render/render_budget.ts RenderBudgetGovernor). Shared by the
// professions fairness guard and the startup graphics safety pins, so the
// scanned set cannot drift between them.
export const PROFILE_TOKENS = [
  'ui_effects_profile',
  'ui_tier_knobs',
  'render_budget',
  'RenderBudgetGovernor',
] as const;
