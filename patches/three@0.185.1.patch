diff --git a/build/three.module.js b/build/three.module.js
index ad13abf7d1..24eae6a19f 100644
--- a/build/three.module.js
+++ b/build/three.module.js
@@ -438,7 +438,7 @@ var morphtarget_pars_vertex = "#ifdef USE_MORPHTARGETS\n\t#ifndef USE_INSTANCING
 
 var morphtarget_vertex = "#ifdef USE_MORPHTARGETS\n\ttransformed *= morphTargetBaseInfluence;\n\tfor ( int i = 0; i < MORPHTARGETS_COUNT; i ++ ) {\n\t\tif ( morphTargetInfluences[ i ] != 0.0 ) transformed += getMorph( gl_VertexID, i, 0 ).xyz * morphTargetInfluences[ i ];\n\t}\n#endif";
 
-var normal_fragment_begin = "float faceDirection = gl_FrontFacing ? 1.0 : - 1.0;\n#ifdef FLAT_SHADED\n\tvec3 fdx = dFdx( vViewPosition );\n\tvec3 fdy = dFdy( vViewPosition );\n\tvec3 normal = normalize( cross( fdx, fdy ) );\n#else\n\tvec3 normal = normalize( vNormal );\n\t#ifdef DOUBLE_SIDED\n\t\tnormal *= faceDirection;\n\t#endif\n#endif\n#if defined( USE_NORMALMAP_TANGENTSPACE ) || defined( USE_CLEARCOAT_NORMALMAP ) || defined( USE_ANISOTROPY )\n\t#ifdef USE_TANGENT\n\t\tmat3 tbn = mat3( normalize( vTangent ), normalize( vBitangent ), normal );\n\t#else\n\t\tmat3 tbn = getTangentFrame( - vViewPosition, normal,\n\t\t#if defined( USE_NORMALMAP )\n\t\t\tvNormalMapUv\n\t\t#elif defined( USE_CLEARCOAT_NORMALMAP )\n\t\t\tvClearcoatNormalMapUv\n\t\t#else\n\t\t\tvUv\n\t\t#endif\n\t\t);\n\t#endif\n\t#ifdef DOUBLE_SIDED\n\t\ttbn[0] *= faceDirection;\n\t\ttbn[1] *= faceDirection;\n\t#endif\n#endif\n#ifdef USE_CLEARCOAT_NORMALMAP\n\t#ifdef USE_TANGENT\n\t\tmat3 tbn2 = mat3( normalize( vTangent ), normalize( vBitangent ), normal );\n\t#else\n\t\tmat3 tbn2 = getTangentFrame( - vViewPosition, normal, vClearcoatNormalMapUv );\n\t#endif\n\t#ifdef DOUBLE_SIDED\n\t\ttbn2[0] *= faceDirection;\n\t\ttbn2[1] *= faceDirection;\n\t#endif\n#endif\nvec3 nonPerturbedNormal = normal;";
+var normal_fragment_begin = "float faceDirection = gl_FrontFacing ? 1.0 : - 1.0;\n#ifdef FLAT_SHADED\n\tvec3 fdx = dFdx( vViewPosition );\n\tvec3 fdy = dFdy( vViewPosition );\n\tvec3 normal = normalize( cross( fdx, fdy ) ); normal = dot( normal, normal ) > 0.0 ? normal : vec3( 0.0, 0.0, 1.0 );\n#else\n\tvec3 normal = normalize( vNormal ); normal = dot( normal, normal ) > 0.0 ? normal : vec3( 0.0, 0.0, 1.0 );\n\t#ifdef DOUBLE_SIDED\n\t\tnormal *= faceDirection;\n\t#endif\n#endif\n#if defined( USE_NORMALMAP_TANGENTSPACE ) || defined( USE_CLEARCOAT_NORMALMAP ) || defined( USE_ANISOTROPY )\n\t#ifdef USE_TANGENT\n\t\tmat3 tbn = mat3( normalize( vTangent ), normalize( vBitangent ), normal );\n\t#else\n\t\tmat3 tbn = getTangentFrame( - vViewPosition, normal,\n\t\t#if defined( USE_NORMALMAP )\n\t\t\tvNormalMapUv\n\t\t#elif defined( USE_CLEARCOAT_NORMALMAP )\n\t\t\tvClearcoatNormalMapUv\n\t\t#else\n\t\t\tvUv\n\t\t#endif\n\t\t);\n\t#endif\n\t#ifdef DOUBLE_SIDED\n\t\ttbn[0] *= faceDirection;\n\t\ttbn[1] *= faceDirection;\n\t#endif\n#endif\n#ifdef USE_CLEARCOAT_NORMALMAP\n\t#ifdef USE_TANGENT\n\t\tmat3 tbn2 = mat3( normalize( vTangent ), normalize( vBitangent ), normal );\n\t#else\n\t\tmat3 tbn2 = getTangentFrame( - vViewPosition, normal, vClearcoatNormalMapUv );\n\t#endif\n\t#ifdef DOUBLE_SIDED\n\t\ttbn2[0] *= faceDirection;\n\t\ttbn2[1] *= faceDirection;\n\t#endif\n#endif\nvec3 nonPerturbedNormal = normal;";
 
 var normal_fragment_maps = "#ifdef USE_NORMALMAP_OBJECTSPACE\n\tnormal = texture2D( normalMap, vNormalMapUv ).xyz * 2.0 - 1.0;\n\t#ifdef FLIP_SIDED\n\t\tnormal = - normal;\n\t#endif\n\t#ifdef DOUBLE_SIDED\n\t\tnormal = normal * faceDirection;\n\t#endif\n\tnormal = normalize( normalMatrix * normal );\n#elif defined( USE_NORMALMAP_TANGENTSPACE )\n\tvec3 mapN = texture2D( normalMap, vNormalMapUv ).xyz * 2.0 - 1.0;\n\t#if defined( USE_PACKED_NORMALMAP )\n\t\tmapN = vec3( mapN.xy, sqrt( saturate( 1.0 - dot( mapN.xy, mapN.xy ) ) ) );\n\t#endif\n\tmapN.xy *= normalScale;\n\tnormal = normalize( tbn * mapN );\n#elif defined( USE_BUMPMAP )\n\tnormal = perturbNormalArb( - vViewPosition, normal, dHdxy_fwd(), faceDirection );\n#endif";
 
@@ -458,7 +458,7 @@ var clearcoat_pars_fragment = "#ifdef USE_CLEARCOATMAP\n\tuniform sampler2D clea
 
 var iridescence_pars_fragment = "#ifdef USE_IRIDESCENCEMAP\n\tuniform sampler2D iridescenceMap;\n#endif\n#ifdef USE_IRIDESCENCE_THICKNESSMAP\n\tuniform sampler2D iridescenceThicknessMap;\n#endif";
 
-var opaque_fragment = "#ifdef OPAQUE\ndiffuseColor.a = 1.0;\n#endif\n#ifdef USE_TRANSMISSION\ndiffuseColor.a *= material.transmissionAlpha;\n#endif\ngl_FragColor = vec4( outgoingLight, diffuseColor.a );";
+var opaque_fragment = "#ifdef OPAQUE\ndiffuseColor.a = 1.0;\n#endif\n#ifdef USE_TRANSMISSION\ndiffuseColor.a *= material.transmissionAlpha;\n#endif\noutgoingLight.x = ( outgoingLight.x < 0.0 || outgoingLight.x >= 0.0 ) ? outgoingLight.x : 0.0;\noutgoingLight.y = ( outgoingLight.y < 0.0 || outgoingLight.y >= 0.0 ) ? outgoingLight.y : 0.0;\noutgoingLight.z = ( outgoingLight.z < 0.0 || outgoingLight.z >= 0.0 ) ? outgoingLight.z : 0.0;\ngl_FragColor = vec4( outgoingLight, diffuseColor.a );";
 
 var packing = "vec3 packNormalToRGB( const in vec3 normal ) {\n\treturn normalize( normal ) * 0.5 + 0.5;\n}\nvec3 unpackRGBToNormal( const in vec3 rgb ) {\n\treturn 2.0 * rgb.xyz - 1.0;\n}\nconst float PackUpscale = 256. / 255.;const float UnpackDownscale = 255. / 256.;const float ShiftRight8 = 1. / 256.;\nconst float Inv255 = 1. / 255.;\nconst vec4 PackFactors = vec4( 1.0, 256.0, 256.0 * 256.0, 256.0 * 256.0 * 256.0 );\nconst vec2 UnpackFactors2 = vec2( UnpackDownscale, 1.0 / PackFactors.g );\nconst vec3 UnpackFactors3 = vec3( UnpackDownscale / PackFactors.rg, 1.0 / PackFactors.b );\nconst vec4 UnpackFactors4 = vec4( UnpackDownscale / PackFactors.rgb, 1.0 / PackFactors.a );\nvec4 packDepthToRGBA( const in float v ) {\n\tif( v <= 0.0 )\n\t\treturn vec4( 0., 0., 0., 0. );\n\tif( v >= 1.0 )\n\t\treturn vec4( 1., 1., 1., 1. );\n\tfloat vuf;\n\tfloat af = modf( v * PackFactors.a, vuf );\n\tfloat bf = modf( vuf * ShiftRight8, vuf );\n\tfloat gf = modf( vuf * ShiftRight8, vuf );\n\treturn vec4( vuf * Inv255, gf * PackUpscale, bf * PackUpscale, af );\n}\nvec3 packDepthToRGB( const in float v ) {\n\tif( v <= 0.0 )\n\t\treturn vec3( 0., 0., 0. );\n\tif( v >= 1.0 )\n\t\treturn vec3( 1., 1., 1. );\n\tfloat vuf;\n\tfloat bf = modf( v * PackFactors.b, vuf );\n\tfloat gf = modf( vuf * ShiftRight8, vuf );\n\treturn vec3( vuf * Inv255, gf * PackUpscale, bf );\n}\nvec2 packDepthToRG( const in float v ) {\n\tif( v <= 0.0 )\n\t\treturn vec2( 0., 0. );\n\tif( v >= 1.0 )\n\t\treturn vec2( 1., 1. );\n\tfloat vuf;\n\tfloat gf = modf( v * 256., vuf );\n\treturn vec2( vuf * Inv255, gf );\n}\nfloat unpackRGBAToDepth( const in vec4 v ) {\n\treturn dot( v, UnpackFactors4 );\n}\nfloat unpackRGBToDepth( const in vec3 v ) {\n\treturn dot( v, UnpackFactors3 );\n}\nfloat unpackRGToDepth( const in vec2 v ) {\n\treturn v.r * UnpackFactors2.r + v.g * UnpackFactors2.g;\n}\nvec4 pack2HalfToRGBA( const in vec2 v ) {\n\tvec4 r = vec4( v.x, fract( v.x * 255.0 ), v.y, fract( v.y * 255.0 ) );\n\treturn vec4( r.x - r.y / 255.0, r.y, r.z - r.w / 255.0, r.w );\n}\nvec2 unpackRGBATo2Half( const in vec4 v ) {\n\treturn vec2( v.x + ( v.y / 255.0 ), v.z + ( v.w / 255.0 ) );\n}\nfloat viewZToOrthographicDepth( const in float viewZ, const in float near, const in float far ) {\n\treturn ( viewZ + near ) / ( near - far );\n}\nfloat orthographicDepthToViewZ( const in float depth, const in float near, const in float far ) {\n\t#ifdef USE_REVERSED_DEPTH_BUFFER\n\t\n\t\treturn depth * ( far - near ) - far;\n\t#else\n\t\treturn depth * ( near - far ) - near;\n\t#endif\n}\nfloat viewZToPerspectiveDepth( const in float viewZ, const in float near, const in float far ) {\n\treturn ( ( near + viewZ ) * far ) / ( ( far - near ) * viewZ );\n}\nfloat perspectiveDepthToViewZ( const in float depth, const in float near, const in float far ) {\n\t\n\t#ifdef USE_REVERSED_DEPTH_BUFFER\n\t\treturn ( near * far ) / ( ( near - far ) * depth - near );\n\t#else\n\t\treturn ( near * far ) / ( ( far - near ) * depth - far );\n\t#endif\n}";
 
@@ -6643,12 +6643,13 @@ function generateCubeUVSize( parameters ) {
 
 }
 
-function WebGLProgram( renderer, cacheKey, parameters, bindingStates ) {
-
-	// TODO Send this event to Three.js DevTools
-	// log( 'WebGLProgram', cacheKey );
-
-	const gl = renderer.getContext();
+// World of ClaudeCraft: the final GLSL, assembled without compiling it. three
+// builds a program's vertex and fragment sources inside the WebGLProgram
+// constructor and links them in the same breath, yet the browser keys its
+// program cache on those exact bytes, so warming a program elsewhere (a worker
+// GL context) needs the sources BEFORE the link. The assembly below is the
+// constructor's own, moved verbatim so both paths cannot drift.
+function assembleProgramGlsl( parameters ) {
 
 	const defines = parameters.defines;
 
@@ -6665,8 +6666,6 @@ function WebGLProgram( renderer, cacheKey, parameters, bindingStates ) {
 
 	const customDefines = generateDefines( defines );
 
-	const program = gl.createProgram();
-
 	let prefixVertex, prefixFragment;
 	let versionString = parameters.glslVersion ? '#version ' + parameters.glslVersion + '\n' : '';
 
@@ -7065,6 +7064,21 @@ function WebGLProgram( renderer, cacheKey, parameters, bindingStates ) {
 
 	const vertexGlsl = versionString + prefixVertex + vertexShader;
 	const fragmentGlsl = versionString + prefixFragment + fragmentShader;
+	return { vertexGlsl: vertexGlsl, fragmentGlsl: fragmentGlsl };
+
+}
+
+function WebGLProgram( renderer, cacheKey, parameters, bindingStates ) {
+
+	// TODO Send this event to Three.js DevTools
+	// log( 'WebGLProgram', cacheKey );
+
+	const gl = renderer.getContext();
+
+	const program = gl.createProgram();
+
+	const { vertexGlsl, fragmentGlsl } = assembleProgramGlsl( parameters );
+
 
 	// log( '*VERTEX*', vertexGlsl );
 	// log( '*FRAGMENT*', fragmentGlsl );
@@ -7989,12 +8003,30 @@ function WebGLPrograms( renderer, environments, extensions, capabilities, bindin
 		return uniforms;
 
 	}
+	// World of ClaudeCraft: released program retention. Upstream destroys a
+	// program the moment its last material lets go, so a material disposed and
+	// re-minted under the same cache key (a streamed prop cell unloading and
+	// reloading, one player of a class leaving and another arriving) links the
+	// same program again, cold, on the main thread. Released programs are kept
+	// LINKED in a bounded FIFO instead, and acquireProgram hands one back as if
+	// it had never left; the oldest is destroyed once the bound is reached. The
+	// bound is a count (a memory bound), never a timer.
+	const RETAINED_PROGRAM_LIMIT = 64;
+	const retainedPrograms = [];
+
 
 	function acquireProgram( parameters, cacheKey ) {
 
 		let program = programsMap.get( cacheKey );
 
 		if ( program !== undefined ) {
+			if ( program.usedTimes === 0 ) {
+
+				const r = retainedPrograms.indexOf( program );
+				if ( r !== - 1 ) retainedPrograms.splice( r, 1 );
+
+			}
+
 
 			++ program.usedTimes;
 
@@ -8010,21 +8042,37 @@ function WebGLPrograms( renderer, environments, extensions, capabilities, bindin
 		return program;
 
 	}
+	// World of ClaudeCraft: cache-key probe. The off-thread shader warm-up must
+	// skip a program this cache has already linked; there is nothing left to warm.
+	function hasProgram( cacheKey ) {
+
+		return programsMap.has( cacheKey );
+
+	}
+
+	function destroyProgram( program ) {
+
+		// Remove from unordered set
+		const i = programs.indexOf( program );
+		programs[ i ] = programs[ programs.length - 1 ];
+		programs.pop();
+
+		// Remove from map
+		programsMap.delete( program.cacheKey );
+
+		// Free WebGL resources
+		program.destroy();
+
+	}
+
 
 	function releaseProgram( program ) {
 
 		if ( -- program.usedTimes === 0 ) {
 
-			// Remove from unordered set
-			const i = programs.indexOf( program );
-			programs[ i ] = programs[ programs.length - 1 ];
-			programs.pop();
+			retainedPrograms.push( program );
 
-			// Remove from map
-			programsMap.delete( program.cacheKey );
-
-			// Free WebGL resources
-			program.destroy();
+			if ( retainedPrograms.length > RETAINED_PROGRAM_LIMIT ) destroyProgram( retainedPrograms.shift() );
 
 		}
 
@@ -8046,10 +8094,13 @@ function WebGLPrograms( renderer, environments, extensions, capabilities, bindin
 		getParameters: getParameters,
 		getProgramCacheKey: getProgramCacheKey,
 		getUniforms: getUniforms,
+		hasProgram: hasProgram,
 		acquireProgram: acquireProgram,
 		releaseProgram: releaseProgram,
 		releaseShaderCache: releaseShaderCache,
 		// Exposed for resource monitoring & error feedback via renderer.info:
+		// Exposed for resource monitoring (World of ClaudeCraft retention):
+		retainedPrograms: retainedPrograms,
 		programs: programs,
 		dispose: dispose
 	};
@@ -9173,6 +9224,38 @@ function WebGLShadowMap( renderer, objects, capabilities ) {
 
 		_state.buffers.depth.setTest( true );
 		_state.setScissorTest( false );
+		// World of ClaudeCraft: colour-write-free shadow depth pass. A RenderTarget
+		// must carry at least one colour attachment, so a non-VSM shadow map
+		// allocates an RGBA8 texture beside its DepthTexture that NOTHING ever
+		// samples: WebGLLights binds shadow.map.depthTexture and
+		// shadowmap_pars_fragment reads it through sampler2DShadow. Upstream let
+		// the depth material write that texture for every rasterized fragment,
+		// so the pass paid full-coverage colour traffic (blending off, but the
+		// ROP write and its bandwidth are real, and they scale with shadow-caster
+		// overdraw) for a buffer with no reader. Closing the colour mask on the
+		// depth and distance materials (getDepthMaterial below) removes it.
+		//
+		// The CLEAR is deliberately left whole. Skipping the colour bit would
+		// look like a further saving, but a clear is the cheap load action
+		// everywhere: a fast clear on an immediate-mode GPU, and loadAction=clear
+		// on a tile-based one, where dropping it turns the attachment into
+		// loadAction=load and makes the pass strictly more expensive. This
+		// renderer's own reference measurements come from a Metal tiler, so the
+		// clear stays.
+		//
+		// Which is exactly why the mask is re-opened BEFORE each clear below and
+		// not only once at the end of the pass. glClear honours the colour write
+		// mask and WebGLRenderer.clear never re-opens it, so once the first
+		// shadow map's depth material has closed the mask, the clear of a SECOND
+		// shadow map in the same frame would be a silent no-op and that map would
+		// keep the previous frame's colour. Only the sun casts here today, so it
+		// is latent; it is still wrong, and it costs one cached state write.
+		//
+		// VSM is untouched: its blur passes write that colour attachment for
+		// real. colorWrite is not a program-cache-key input (WebGLPrograms never
+		// reads it), so no program key moves and no material relinks.
+		const colorWriteFreePass = this.type !== VSMShadowMap;
+
 
 		// check for shadow map type changes
 
@@ -9336,6 +9419,7 @@ function WebGLShadowMap( renderer, objects, capabilities ) {
 				if ( shadow.map.isWebGLCubeRenderTarget ) {
 
 					renderer.setRenderTarget( shadow.map, face );
+					if ( colorWriteFreePass ) _state.buffers.color.setMask( true );
 					renderer.clear();
 
 				} else {
@@ -9344,6 +9428,7 @@ function WebGLShadowMap( renderer, objects, capabilities ) {
 					if ( face === 0 ) {
 
 						renderer.setRenderTarget( shadow.map );
+						if ( colorWriteFreePass ) _state.buffers.color.setMask( true );
 						renderer.clear();
 
 					}
@@ -9416,6 +9501,12 @@ function WebGLShadowMap( renderer, objects, capabilities ) {
 		_previousType = this.type;
 
 		scope.needsUpdate = false;
+		// World of ClaudeCraft: the depth materials leave the colour mask closed,
+		// and a masked glClear is silently a no-op. three's own frame clear
+		// re-opens the mask first (WebGLBackground), but the pass restores what
+		// it changed rather than leaving that to every caller.
+		if ( colorWriteFreePass ) _state.buffers.color.setMask( true );
+
 
 		renderer.setRenderTarget( currentRenderTarget, activeCubeFace, activeMipmapLevel );
 
@@ -9516,6 +9607,11 @@ function WebGLShadowMap( renderer, objects, capabilities ) {
 
 		result.visible = material.visible;
 		result.wireframe = material.wireframe;
+		// World of ClaudeCraft: depth-only shadow pass (see this.render). Set
+		// here rather than on the two shared materials so the alpha-test clones
+		// in _materialCache and any customDepthMaterial follow the pass too,
+		// alongside the other fields the pass already owns on them.
+		result.colorWrite = type === VSMShadowMap;
 
 		if ( type === VSMShadowMap ) {
 
@@ -16461,6 +16557,7 @@ class WebGLRenderer {
 			bufferRenderer = new WebGLBufferRenderer( _gl, extensions, info );
 			indexedBufferRenderer = new WebGLIndexedBufferRenderer( _gl, extensions, info );
 
+			info.retainedPrograms = programCache.retainedPrograms;
 			info.programs = programCache.programs;
 
 			/**
@@ -17463,6 +17560,188 @@ class WebGLRenderer {
 			return materials;
 
 		};
+		// World of ClaudeCraft: the exact GLSL compile() would link, collected
+		// without linking anything. The browser keys its program cache on the
+		// final source bytes, so an off-thread warm-up has to know them first.
+		// This mirrors compile() step for step and shares its caller contract:
+		// the renderer state the real link will see (bound render target, scene
+		// fog) is the caller's responsibility here exactly as it is there.
+		/**
+		 * Collects the final vertex and fragment GLSL of every program {@link WebGLRenderer#compile}
+		 * would acquire for the given scene, without compiling or linking any of them. Programs this
+		 * renderer has already linked are skipped.
+		 *
+		 * @param {Object3D} scene - The scene or another type of 3D object to collect from.
+		 * @param {Camera} camera - The camera.
+		 * @param {?Scene} [targetScene=null] - The target scene.
+		 * @return {Array<Object>} One entry per distinct unlinked program, in traversal order.
+		 */
+		this.collectProgramSources = function ( scene, camera, targetScene = null ) {
+
+			const collected = [];
+			const collectedKeys = new Set();
+
+			function collectMaterialSources( material, scene, object ) {
+
+				if ( scene.isScene !== true ) scene = _emptyScene; // scene could be a Mesh, Line, Points, ...
+
+				const lights = currentRenderState.state.lights;
+				const shadowsArray = currentRenderState.state.shadowsArray;
+
+				const parameters = programCache.getParameters( material, lights.state, shadowsArray, scene, object, currentRenderState.state.lightProbeGridArray );
+				const cacheKey = programCache.getProgramCacheKey( parameters );
+
+				if ( collectedKeys.has( cacheKey ) ) return;
+				if ( programCache.hasProgram( cacheKey ) ) return;
+
+				collectedKeys.add( cacheKey );
+
+				// The dispose listener getProgram registers on a new material, so a
+				// material only ever collected here releases its shader cache entry too.
+				material.addEventListener( 'dispose', onMaterialDispose );
+
+				parameters.uniforms = programCache.getUniforms( material );
+
+				// getProgram builds node materials here first; this renderer has none, so
+				// the mirror does not carry that branch (a node material would assemble wrong).
+				material.onBeforeCompile( parameters, _this );
+
+				const glsl = assembleProgramGlsl( parameters );
+
+				collected.push( {
+					material: material,
+					object: object,
+					cacheKey: cacheKey,
+					name: parameters.shaderName,
+					vertexGlsl: glsl.vertexGlsl,
+					fragmentGlsl: glsl.fragmentGlsl,
+					index0Attribute: parameters.index0AttributeName !== undefined ? parameters.index0AttributeName : ( parameters.hasPositionAttribute === true ? 'position' : '' )
+				} );
+
+			}
+
+			// The side branch of prepareMaterial(), mirrored: a transparent
+			// double-sided material without forceSinglePass links a BackSide and a
+			// FrontSide program (the side feeds the cache key), so the collection
+			// reads both. The side is only borrowed; nothing here marks the material
+			// for an update.
+			function collectPreparedMaterial( material, scene, object ) {
+
+				if ( material.transparent === true && material.side === DoubleSide && material.forceSinglePass === false ) {
+
+					try {
+
+						material.side = BackSide;
+						collectMaterialSources( material, scene, object );
+
+						material.side = FrontSide;
+						collectMaterialSources( material, scene, object );
+
+					} finally {
+
+						material.side = DoubleSide;
+
+					}
+
+				} else {
+
+					collectMaterialSources( material, scene, object );
+
+				}
+
+			}
+
+			if ( targetScene === null ) targetScene = scene;
+
+			currentRenderState = renderStates.get( targetScene );
+			currentRenderState.init( camera );
+
+			renderStateStack.push( currentRenderState );
+
+			try {
+
+				// gather lights from both the target scene and the new object that will be added to the scene.
+
+				targetScene.traverseVisible( function ( object ) {
+
+					if ( object.isLight && object.layers.test( camera.layers ) ) {
+
+						currentRenderState.pushLight( object );
+
+						if ( object.castShadow ) {
+
+							currentRenderState.pushShadow( object );
+
+						}
+
+					}
+
+				} );
+
+				if ( scene !== targetScene ) {
+
+					scene.traverseVisible( function ( object ) {
+
+						if ( object.isLight && object.layers.test( camera.layers ) ) {
+
+							currentRenderState.pushLight( object );
+
+							if ( object.castShadow ) {
+
+								currentRenderState.pushShadow( object );
+
+							}
+
+						}
+
+					} );
+
+				}
+
+				currentRenderState.setupLights();
+
+				// Only collect from the new scene, not the targetScene.
+
+				scene.traverse( function ( object ) {
+
+					if ( ! ( object.isMesh || object.isPoints || object.isLine || object.isSprite ) ) {
+
+						return;
+
+					}
+
+					const material = object.material;
+
+					if ( material ) {
+
+						if ( Array.isArray( material ) ) {
+
+							for ( let i = 0; i < material.length; i ++ ) {
+
+								collectPreparedMaterial( material[ i ], targetScene, object );
+
+							}
+
+						} else {
+
+							collectPreparedMaterial( material, targetScene, object );
+
+						}
+
+					}
+
+				} );
+
+			} finally {
+
+				currentRenderState = renderStateStack.pop();
+
+			}
+
+			return collected;
+
+		};
+
 
 		// compileAsync
 
@@ -17486,22 +17765,82 @@ class WebGLRenderer {
 			// ready to be used before resolving the promise.
 
 			return new Promise( ( resolve ) => {
+				// World of ClaudeCraft: bounded isReady poll pass. Each
+				// COMPLETION_STATUS_KHR query is a synchronous GPU-process
+				// round-trip; with hundreds of pending programs and a
+				// link-backlogged GPU process, one unbounded forEach pass
+				// measured 10.2 s on the production main thread. One pass now
+				// checks pending materials round-robin under a small time
+				// budget (always at least one), so readiness detection
+				// amortizes across passes while links settle off-thread.
+				// The reschedule interval BACKS OFF when a pass was expensive
+				// and made no progress: every concurrent compileAsync promise
+				// owns its own poll timer, and under a link backlog N pollers
+				// each paying a ~30 ms query every 10 ms owned the whole main
+				// thread (measured as sub-1-fps frames locally). Expensive
+				// queries mean readiness is far anyway; the interval resets
+				// the moment a pass progresses or queries come back cheap.
+				const POLL_PASS_BUDGET_MS = 2;
+				const POLL_INTERVAL_MIN_MS = 10;
+				const POLL_INTERVAL_MAX_MS = 320;
+				let pollCursor = 0;
+				let pollIntervalMs = POLL_INTERVAL_MIN_MS;
+
 
 				function checkMaterialsReady() {
 
-					materials.forEach( function ( material ) {
+					const pending = Array.from( materials );
+					const passStart = performance.now();
+
+					// World of ClaudeCraft: dedupe COMPLETION_STATUS_KHR queries by
+					// program within a pass. Materials share linked programs through
+					// the program cache, and isReady() only caches a POSITIVE result:
+					// while a shared program is still linking, every material holding
+					// it repaid the synchronous GPU-process query in the same pass.
+					// One not-ready verdict per program now covers all its materials
+					// for the rest of the pass (ready programs stay cheap through
+					// isReady()'s own positive cache), so a budgeted pass spends its
+					// 2 ms on DISTINCT pending programs instead of repeat queries.
+					const notReadyThisPass = new Set();
+					for ( let i = 0; i < pending.length; i ++ ) {
+
+						const material = pending[ ( pollCursor + i ) % pending.length ];
 
 						const materialProperties = properties.get( material );
 						const program = materialProperties.currentProgram;
 
-						if ( program.isReady() ) {
+						// World of ClaudeCraft: three compileAsync disposal race (r165, still
+						// present in r185). If churn releases renderer properties during this
+						// timer poll, no program remains to wait for. Removing the material
+						// avoids the isReady TypeError and lets the promise settle. Recheck on
+						// upgrade.
+						if ( program === undefined ) {
 
-							// remove any programs that report they're ready to use from the list
 							materials.delete( material );
+						} else if ( notReadyThisPass.has( program ) === false ) {
+
+							if ( program.isReady() ) {
+
+								// remove any programs that report they're ready to use from the list
+								materials.delete( material );
+
+							} else {
+
+								notReadyThisPass.add( program );
+
+							}
+
 
 						}
 
-					} );
+						if ( performance.now() - passStart >= POLL_PASS_BUDGET_MS && i + 1 < pending.length ) {
+
+							pollCursor = ( pollCursor + i + 1 ) % pending.length;
+							break;
+
+						}
+
+					}
 
 					// once the list of compiling materials is empty, call the callback
 
@@ -17512,9 +17851,19 @@ class WebGLRenderer {
 
 					}
 
-					// if some materials are still not ready, wait a bit and check again
+					// if some materials are still not ready, wait a bit and check again.
+					// The reset keys on CHEAP QUERIES ONLY, deliberately not on progress:
+					// with ~30 ms queries a poller that finds one ready material per pass
+					// would reset to 10 ms every time and still own ~75 percent of the
+					// main thread while draining. Expensive queries back off whether or
+					// not they progressed; readiness latency is bounded by the cap.
 
-					setTimeout( checkMaterialsReady, 10 );
+					const passMs = performance.now() - passStart;
+					pollIntervalMs =
+						passMs < POLL_PASS_BUDGET_MS
+							? POLL_INTERVAL_MIN_MS
+							: Math.min( POLL_INTERVAL_MAX_MS, pollIntervalMs * 2 );
+					setTimeout( checkMaterialsReady, pollIntervalMs );
 
 				}
 
@@ -17876,7 +18225,11 @@ class WebGLRenderer {
 
 				} else if ( object.isMesh || object.isLine || object.isPoints ) {
 
-					if ( ! object.frustumCulled || _frustum.intersectsObject( object ) ) {
+					// World of ClaudeCraft: a count 0 InstancedMesh draws nothing, yet the render
+					// list still reaches setProgram, which LINKS its program for zero pixels.
+					const drawsNothing = object.isInstancedMesh === true && object.count === 0;
+
+					if ( drawsNothing === false && ( ! object.frustumCulled || _frustum.intersectsObject( object ) ) ) {
 
 						const geometry = objects.update( object );
 						const material = object.material;
