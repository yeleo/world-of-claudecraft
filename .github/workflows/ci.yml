name: CI

on:
  pull_request:
  # The merge queue on main and release/** tests each candidate merge result on
  # a merge_group event. This trigger is load-bearing: without it the required
  # checks never report on queue runs and every queued PR stalls until the
  # queue's status-check timeout rejects it. Queue settings live in the repo's
  # rulesets (not in git); docs/merge-queue.md is the operator note.
  merge_group:
  push:
    branches:
      - main
      - 'dev-*'
      - 'release/**'
  workflow_dispatch:

permissions:
  contents: read

# Cancel superseded pull_request runs for the same PR. Group by event name plus
# PR number (or ref for pushes) so release/** pushes and other events stay
# isolated from ordinary PR traffic and cannot cancel each other (D4).
# cancel-in-progress is true for every event in this group; release isolation
# comes from the event_name segment, not from disabling cancel on push.
# merge_group runs key on github.ref, which is the queue's own
# gh-readonly-queue/<branch>/pr-<n>-<sha> ref: unique per live merge group, so
# cancel-in-progress cannot cross-cancel two live groups (the queue dissolves a
# group before any ref could be reused), and the event_name segment keeps queue
# runs from ever colliding with PR or push runs. The flip side of unique refs:
# a dissolved group's superseded run is never cancelled by this group either;
# that cleanup is GitHub's, so the trade costs at most idle runner minutes,
# never a live group's required checks.
concurrency:
  group: ${{ github.workflow }}-${{ github.event_name }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

# Abort dead checkout transfers fast instead of letting them ride a job bound
# to its kill. The runner-side checkout-stall class (the timeout-minutes
# rationale on each job below) presents as a git fetch at ZERO throughput for
# tens of minutes: run 31392590628 (2026-08-10) bound-killed the same lane
# three attempts in a row, at its then-20-minute bound each time, on fetches
# that produced nothing and then completed moments after the kill. git's own
# low-speed abort ends any HTTP transfer whose received payload stays under
# 1000 bytes per second for 120 consecutive seconds (mechanism proven: a live
# HTTPS fetch under an absurd floor dies in 2 s with curl 28), and
# actions/checkout then retries the fetch in-step (3 attempts, 10 to 20
# second backoff). HONEST LIMIT, measured, not assumed: the floor polices an
# in-progress transfer that slows to a trickle, but the hang variant observed
# on 2026-08-10 (run 31402711619: four fetches sat 19m52s producing NOTHING,
# WITH this floor confirmed in each step's env) dies in a phase curl's speed
# timer never polices, so this env block did not catch it. Keep it (it is
# free and covers the trickle variant); the reliable net for the hang variant is
# ci-stall-rerun.yml, which reruns the run's failed jobs once (nothing
# after the dead checkout ran, so nothing can be masked). Workflow-wide on
# purpose: it also covers the lint job's base-ref fetch and the changes
# job's sparse checkout, and it is inert for every non-HTTP git call (the
# git-subprocess test fixtures run on local disk and never engage
# git-remote-https). A healthy fetch of this repository runs orders of
# magnitude above the floor, so a merely slow transfer is never clipped.
# Accepted tradeoff: the bare git fetches in the lint job's "Determine base
# ref" step gain the same floor with no retry wrapper and no auto-rerun
# coverage (not a checkout step), so a trickle-abort there fails the
# minutes-long lint job fast and takes a manual rerun; that beats a
# 15-minute bound ride. Values pinned by tests/ci_workflow.test.ts.
env:
  GIT_HTTP_LOW_SPEED_LIMIT: '1000'
  GIT_HTTP_LOW_SPEED_TIME: '120'

# Two-tier i18n gate, split by git ref in this one file. Each tier is a parallel
# pair (tests job + checks job, no needs edge either way) so wall clock is
# max(tests, checks), not their sum. Locally `npm run gate` (scripts/gate.mjs)
# still runs the combined step list serially with ONE full unsharded vitest.
#
#   pr-gate       cheap, English-only-legal TEST job. Runs on pull requests, on
#                 pushes to non-release branches (main, dev-*), and on manual
#                 dispatch. It runs `npm test` WITHOUT I18N_RELEASE_TIER, so the
#                 release-only checks (s3_localized, copied-English rendered
#                 content, 21-locale key parity, empty-`pending`) are skipped.
#                 What it still enforces: registry-in-sync, s3_registered (every
#                 emit maps to a registered key/RULE), and placeholder parity for
#                 the translations that actually exist. An English-only PR
#                 (sparse overlay) passes here. The suite is an 8-shard matrix
#                 (each shard executes a partition of the test files), so the
#                 job's wall time is the slowest shard, not the whole suite.
#
#   pr-long-sims-a/-b  the long-sims lane pair (Phase 4 of the CI/CD
#                 performance packet, split in two by the lane-diet PR): the
#                 CI_LONG_SUITES rotation sims (scripts/lib/ci_shard_plan.mjs)
#                 each cost 90+ seconds inside a shard and were setting the
#                 slowest shard's wall clock, so these two jobs run them alone
#                 (one CI_LONG_SUITE_HALVES half each) while every pr-gate
#                 shard leg excludes them. Same triggers and filters as
#                 pr-gate; mode for mode, the ten PR-tier test jobs together
#                 run exactly what the pre-lane 8-shard layout would have run
#                 (pinned by tests/ci_shard_plan.test.ts).
#
#   pr-checks     the PR tier's serialized checks (i18n freshness + coverage
#                 summary, malware gate, game/admin typecheck, env/server/bot/client
#                 builds), parallel with pr-gate. Same triggers as pr-gate.
#
#   release-gate  full TEST job for a release ref. Runs on release/** pushes and on
#                 the release-to-main PR merge result. Runs at PR tier (no
#                 I18N_RELEASE_TIER), same 8-shard matrix as pr-gate, tests only
#                 (no builds), so a red shard here always means a real regression.
#
#   release-i18n  the release TIER, alone: the suites that read
#                 I18N_RELEASE_TIER, run under it, unsharded. A merely `pending`
#                 (untranslated) key cannot survive this job. Expected red until
#                 the release-time locale fill, which is exactly why it is its own
#                 job: on release-gate it used to mask genuine failures (#2820).
#
#   release-checks the release tier's serialized checks (same step list as
#                 pr-checks), parallel with release-gate. Same triggers as
#                 release-gate. Does not set I18N_RELEASE_TIER (freshness,
#                 malware, typecheck, and builds do not need it; release-i18n
#                 alone carries the tier flag).
#
# The release tier is a superset: release-gate runs every PR-tier check and
# release-i18n adds the release-only assertions on top; typecheck and the four
# builds live in release-checks.
#
# Path filters (D10): a cheap `changes` job classifies pull_request diffs as
# code vs docs-only. Docs/markdown/screenshot-only PRs keep lint (and any
# other always-on cheap jobs) and skip the PR-tier work: pr-long-sims-a/-b,
# pr-checks, and browser-gate skip at JOB level, while pr-gate's matrix legs
# start and skip at STEP level so all eight suffixed required check runs
# still report (see the pr-gate job comment for why that difference is
# load-bearing). Any touch of the code path set forces the full PR tier. push
# to main/dev-*, workflow_dispatch, and merge_group queue runs always report
# code=true (full PR tier when unsure).
# Release/** pushes and release-to-main PRs never consult the filter:
# release-gate, release-checks, and release-version-gate stay always-full
# (no path skip on those jobs).
#
# Selective PR tier (the CI/CD performance packet's Phase 2): the same
# changes job also emits test_mode/changed_files from the same listing, and
# the pr-gate shards run scripts/ci_shard_test.mjs, which either replays the
# pre-selection `npm test -- --shard` step minus the long-sims lane files
# (mode=full, the fail-closed default for anything unprovable; the lane job
# runs those files in the same run) or runs the always-run floor +
# `vitest related`, sharded. The reference semantics and the audit contract
# live in docs/qa-gate.md ("Selective PR-tier CI"). Selection NEVER applies
# to release-gate or the nightly workflow: those are the unconditional
# full-suite backstops.
#
# Merge queue runs: a merge_group event routes through the PR-tier jobs
# (changes, lint, pr-gate, pr-long-sims-a/-b, pr-checks, browser-gate). The
# changes job treats every non-PR event as code=true and test_mode=full, so
# the queue always runs the FULL suite on the exact merge result it is about
# to make the branch tip.
# That is deliberate, not an optimization target: the queue is the last
# pre-merge bar, and selection never applies to it. This also covers a queued
# release-to-main PR (the pull_request-only exclusions below cannot match a
# merge_group event, so the full PR tier runs on its merge group too). The
# release lanes' if arms match pull_request and push only, so they stay off
# queue runs; the release/** push that fires after the queue merges remains
# the unconditional full-suite backstop on the real tip.

jobs:
  release-version-gate:
    name: Release version gate
    if: (github.event_name == 'pull_request' && github.base_ref == 'main' && startsWith(github.head_ref, 'release/')) || (github.event_name == 'push' && startsWith(github.ref, 'refs/heads/release/'))
    # Checkout, pnpm install, and one small version-surface check: comparable
    # in shape to release-i18n's toolchain overhead. 10 gives ample margin
    # over a script that runs in a fraction of a second locally.
    timeout-minutes: 10
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v6
        with:
          # Blob-less shallow sparse checkout: scripts/release_version.mjs
          # only imports node:fs/node:path/node:url and only reads/writes
          # package.json, android/app/build.gradle,
          # ios/App/App.xcodeproj/project.pbxproj, src/game/desktop_download.ts,
          # index.html, play.html, README.md, and the files directly under
          # docs/i18n/, never the rest of the tree or the history.
          filter: blob:none
          sparse-checkout: |
            scripts
            package.json
            android/app
            ios/App/App.xcodeproj
            src/game
            index.html
            play.html
            README.md
            docs/i18n

      - name: Verify release version surfaces
        # The script is stdlib-only (fs/path/url), so it runs on the
        # runner's preinstalled Node: no setup-node, no pnpm, no dependency
        # install on this job.
        run: node scripts/release_version.mjs check

  # Classify whether this run touches the code path set (D10). The classifier
  # reads the PR's changed files from the GitHub API (the pull request files
  # endpoint, paginated) instead of a checkout + git diff: the old full-history
  # checkout of this binary-heavy repo cost about 10 serial minutes on every PR
  # before any test could start, and twice wedged for 90+ minutes with no
  # timeout to kill it. Default code=true (fail closed toward the full suite):
  # any API error, pagination overflow, payload mismatch, or non-PR event
  # yields code=true; only a provably docs-only PR sets code=false. The rules
  # and the fail-closed decision live in scripts/lib/ci_change_classify.mjs
  # (unit-tested by tests/ci_change_classify.test.ts). Release jobs never need
  # this job and never read its output.
  changes:
    name: Classify changes
    runs-on: ubuntu-latest
    # Every PR-tier job waits on this one, so a wedged runner must fail fast
    # and be retried, never hang for an hour and a half. 8, not 5: the
    # Phase 6 replay found a healthy 4.42 minute run on this serial critical
    # path (run 31117143257 attempt 1, carrying a 4.00 minute runner setup
    # spike, which counts inside the bound), an 88 percent fill of the old
    # bound where a false kill costs a whole re-run; still single-digit per
    # the Phase 1 acceptance.
    timeout-minutes: 8
    # Job-scoped read-only widen: once workflow-level permissions are declared,
    # unlisted scopes are none, and the PR files endpoint needs
    # pull-requests: read. Nothing here may ever be a write scope.
    permissions:
      contents: read
      pull-requests: read
    outputs:
      code: ${{ steps.filter.outputs.code }}
      # Phase 2 selection decision (docs/qa-gate.md, "Selective PR-tier CI"): the
      # same fetched listing also decides whether pr-gate may run the selective
      # test plan. Fail closed: anything unprovable is test_mode=full, which
      # runs the pre-selection shard step minus the long-sims lane files (the
      # lane job runs those in the same run). Release jobs never read these.
      test_mode: ${{ steps.filter.outputs.test_mode }}
      test_mode_reason: ${{ steps.filter.outputs.test_mode_reason }}
      changed_files: ${{ steps.filter.outputs.changed_files }}
    steps:
      - name: Check out classifier script
        uses: actions/checkout@v6
        with:
          # Blob-less shallow sparse checkout: this job needs only the
          # classifier script, never the tree or the history.
          filter: blob:none
          sparse-checkout: |
            scripts

      - name: Classify changed paths
        id: filter
        env:
          GITHUB_TOKEN: ${{ github.token }}
        # The classifier is stdlib-only (global fetch), so it runs on the
        # runner's preinstalled Node: no setup-node, no dependency install on
        # this serial critical path.
        run: node scripts/detect_code_changes.mjs

  # Biome formatter + linter, adopted as a forward ratchet: only files actually
  # changed by this push/PR are checked (`--changed`), so the large existing tree
  # is never retroactively reformatted, while every new diff must pass format+lint.
  # `--no-errors-on-unmatched` keeps the job green when a change touches no
  # Biome-eligible file (docs, assets, generated i18n).
  lint:
    name: Lint (changed files)
    # Unmatrixed single toolchain-setup-plus-checks job, comparable in shape to
    # browser-gate: checkout, pnpm install, a base-ref fetch, then one biome
    # pass. 15 keeps a margin over browser-gate's 10 minute bound for the extra
    # base-ref resolution step.
    timeout-minutes: 15
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        # Default shallow checkout. Biome --changed --since only needs the base
        # commit object, which the next step fetches with --depth=1 when missing.
        # Full-history checkout was pure waste: it dominated the job wall time.
        uses: actions/checkout@v6

      - name: Set up pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.34.5

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 26
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Determine base ref
        id: base
        env:
          EVENT_NAME: ${{ github.event_name }}
          BASE_REF: ${{ github.base_ref }}
          BEFORE_SHA: ${{ github.event.before }}
          MERGE_GROUP_BASE_SHA: ${{ github.event.merge_group.base_sha }}
          MERGE_GROUP_BASE_REF: ${{ github.event.merge_group.base_ref }}
          DEFAULT_BRANCH: ${{ github.event.repository.default_branch }}
        run: |
          zeros='0000000000000000000000000000000000000000'
          if [ "$EVENT_NAME" = "pull_request" ]; then
            git fetch --no-tags --depth=1 origin "$BASE_REF"
            echo "ref=origin/$BASE_REF" >> "$GITHUB_OUTPUT"
          elif [ "$EVENT_NAME" = "merge_group" ]; then
            # A queue run diffs the merge group against the target-branch tip it
            # was built on, and a merge_group event must NEVER fall through to
            # the default-branch arm: that would diff a release/** group against
            # main and drag the whole release delta (including the
            # intentionally-red whole-repo debt) into biome. So this arm keys on
            # the event alone, and anything unusable about the SHA lands in the
            # fallback below, never outside the arm. The shape guard only
            # selects the SHA path (grep matches per line; the backstop for a
            # malformed value is the fetch itself, since nothing is written to
            # GITHUB_OUTPUT unless its fetch succeeded).
            if printf '%s' "$MERGE_GROUP_BASE_SHA" | grep -qE '^[0-9a-f]{40}$' \
              && git fetch --no-tags --depth=1 origin "$MERGE_GROUP_BASE_SHA"; then
              echo "ref=$MERGE_GROUP_BASE_SHA" >> "$GITHUB_OUTPUT"
            else
              # Malformed or unreachable base SHA (a dissolved predecessor
              # group, a transient fetch failure): fall back to the live
              # target-branch tip, still never the default branch, so one flaky
              # fetch cannot eject a good PR from the queue. An empty base ref
              # has no safe fallback at all: the bare [ -n ] test fails the
              # step loudly (fetching "" would quietly succeed and write a
              # broken ref=origin/ output instead).
              base_branch="${MERGE_GROUP_BASE_REF#refs/heads/}"
              [ -n "$base_branch" ]
              git fetch --no-tags --depth=1 origin "$base_branch"
              echo "ref=origin/$base_branch" >> "$GITHUB_OUTPUT"
            fi
          elif [ -n "$BEFORE_SHA" ] && [ "$BEFORE_SHA" != "$zeros" ]; then
            # Shallow checkout is tip-only; fetch the pre-push tip alone.
            if ! git cat-file -e "${BEFORE_SHA}^{commit}" 2>/dev/null; then
              git fetch --no-tags --depth=1 origin "$BEFORE_SHA"
            fi
            echo "ref=$BEFORE_SHA" >> "$GITHUB_OUTPUT"
          elif git rev-parse --verify --quiet HEAD~1 >/dev/null; then
            echo "ref=HEAD~1" >> "$GITHUB_OUTPUT"
          else
            # New branch / missing before SHA: compare against default branch tip.
            git fetch --no-tags --depth=1 origin "$DEFAULT_BRANCH"
            echo "ref=origin/$DEFAULT_BRANCH" >> "$GITHUB_OUTPUT"
          fi

      - name: Biome check (changed files only)
        env:
          BASE_REF_RESOLVED: ${{ steps.base.outputs.ref }}
        run: npx @biomejs/biome ci --changed --since="$BASE_REF_RESOLVED" --no-errors-on-unmatched

  browser-gate:
    name: Browser tests
    # Skip on confirmed docs-only PRs (no browser tests/code touched). Non-PR
    # and code PRs keep the job (changes.code defaults true off PR events).
    # != 'false', not == 'true': only an explicit docs-only verdict may skip.
    # A missing or empty output must run the job, matching the classifier's own
    # fail-closed doctrine; == 'true' is the one place doubt would fail toward
    # SKIP, and under the merge queue a skipped required check reads satisfied.
    # This covers a green-but-empty output only: a FAILED changes job still
    # skips dependents through needs regardless of this expression, which is
    # why "Classify changes" must itself be a required check.
    needs: changes
    if: needs.changes.outputs.code != 'false'
    # Fail fast on the checkout-stall class (rationale and evidence on the
    # pr-gate bound): the slowest healthy wall observed in the 24 hour
    # replay is 3.73 minutes (run 31053693012), and that sample already
    # contains the slowest observed cold Playwright Chromium install (2.33
    # minutes), so 10 keeps a 2.7x margin over a cold-cache day. Its one
    # stalled run sat 17.5 minutes in checkout inside an 18.4 minute wall
    # (run 31107474546).
    timeout-minutes: 10
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Set up pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.34.5

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 26
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Resolve Playwright version for cache key
        id: playwright-version
        run: echo "version=$(node -p "require('playwright/package.json').version")" >> "$GITHUB_OUTPUT"

      - name: Cache Playwright Chromium browsers
        # Restores ~/.cache/ms-playwright when the Playwright version matches.
        # The install step always runs: on a hit it is a cheap no-op for the
        # browser download and only ensures OS deps; on a miss it downloads.
        # No restore-keys: a version-prefix fallback would still restore a
        # stale Chromium build. npx playwright install then throws it away
        # (playwright's own browser GC deletes any revision the resolved
        # version does not claim), so all the fallback buys is a wasted
        # restore and extract on every version bump.
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-${{ runner.os }}-${{ steps.playwright-version.outputs.version }}

      - name: Install Chromium
        # The old single-step form folded a package-manager run (playwright
        # install-deps) into the browser install, and in practice that half
        # was the step's only failure mode: three consecutive merge-queue
        # rejections on 2026-08-19 died at zero mirror throughput with the
        # browser itself cache-hit. The halves are split so the browser
        # install keeps its hard failure while the OS-dep half is bounded
        # (-k so a TERM-ignoring hang cannot defeat the bound) and verified
        # by CAPABILITY instead of by tool success: the runner image already
        # ships Chromium's shared libraries (a genuinely missing one fails
        # loudly at browser launch in the next step), and the one thing
        # install-deps alone was providing is font coverage, proven when the
        # first PR run of this split shipped without it and only the CJK
        # sprite tests went red (run 32233898992, "黒石の城" measured zero
        # ink). So when install-deps fails, the step checks for CJK fonts
        # directly, retries a targeted font install off the primary archive
        # mirror (the stall class lives on the azure mirror), and fails
        # LOUDLY, still inside this setup step's auto-rerunnable class,
        # only when no route produced the fonts the suite demonstrably
        # needs. Worst-case degraded wall is about 3.7 minutes on top of
        # the 3.73-minute healthy wall, inside this job's 10-minute bound.
        run: |
          npx playwright install chromium
          if ! timeout -k 15 100 npx playwright install-deps chromium; then
            echo "install-deps unavailable (exit $?); verifying CJK font coverage directly"
            if ! fc-list :lang=ja | grep -q .; then
              sudo sed -i 's|azure.archive.ubuntu.com|archive.ubuntu.com|g' /etc/apt/sources.list.d/ubuntu.sources /etc/apt/sources.list 2>/dev/null || true
              timeout -k 15 60 sudo apt-get update || true
              timeout -k 15 90 sudo apt-get install -y --no-install-recommends fonts-noto-cjk || true
            fi
            fc-list :lang=ja | grep -q . || { echo "no CJK font coverage by any route; the browser suite cannot pass without it"; exit 1; }
          fi

      - name: Run browser regressions
        run: npm run test:browser

  # The PR tier is two parallel jobs: pr-gate runs the test suite while pr-checks
  # runs the serialized checks, so the PR critical path is max(tests, checks)
  # instead of their sum. pr-gate additionally fans the suite across an 8-shard
  # matrix (vitest --shard=i/8, packed by scripts/ci_balanced_sequencer.mjs:
  # LPT over measured per-file durations since 2026-08-14; the D11-era LPT
  # and stripe missed the bar on static weights and default sha1-contiguous
  # packing ran until then). Wall time is the slowest shard. Locally
  # `npm run gate` (scripts/gate.mjs) runs the SAME combined step list serially
  # with ONE full unsharded vitest run by design (bounded workers); keep that
  # script's step list in sync when adding or removing a step in either job.
  pr-gate:
    name: PR tests
    # Two-level gate, split on purpose (docs/merge-queue.md, "The required-check
    # contract"). The JOB if carries only the event routing, so every
    # pull_request and merge_group run expands the matrix and reports all eight
    # suffixed check runs; the STEP gate on every step below carries the
    # release-to-main exclusion and the code path filter (D10), so a leg the
    # run does not apply to starts, skips all of its steps, and completes green
    # in seconds. A job-level skip of a MATRIX job would instead collapse to
    # ONE check run named without the (N) suffix, which string-matches none of
    # the required shard contexts and leaves them "expected" forever: under the
    # branch ruleset a docs-only or release-to-main PR could then never be
    # queued or merged (observed live on the Phase 3 queue drills, PR #3038:
    # "8 of 13 required status checks are expected"). Do not drop either step
    # gate arm: release-to-main must still select release-*, and docs-only PRs
    # must still skip the suite work. needs.changes only; no needs edge to
    # pr-checks. merge_group always runs the work (the queue bar is the full
    # PR tier): the release arm cannot match the event, and the classifier
    # always reports code=true off a pull_request event, so neither gate arm
    # can gate off a queue run.
    needs: changes
    if: github.event_name == 'pull_request' || (github.event_name == 'push' && !startsWith(github.ref, 'refs/heads/release/')) || github.event_name == 'workflow_dispatch' || github.event_name == 'merge_group'
    # Fail fast on the runner-side checkout-stall class instead of burning
    # tail: on 2026-08-06 thirteen shard, lane, and browser jobs across seven
    # runs sat 9.6 to 24.4 minutes inside actions/checkout before completing
    # (healthy checkout is under 3 minutes; worst stalled wall 32.8 minutes,
    # run 31111919431 attempt 1), and the stall, not any test, set those
    # runs' tails. The original 20 was sized from a 13.6 minute healthy
    # GREEN worst case (the Phase 2 content-only selective drill, PR #2991)
    # and an 18.6 minute healthy RED one (run 31067745370), and two
    # independent 24 hour replays then found zero healthy jobs over that
    # bound. That zero did not survive sim-deep selective PRs. Selective
    # mode always runs the computed blind/partial floor (779 of the 2674
    # suite files when this was sized; the floor is recomputed in-job, see
    # scripts/lib/test_visibility.mjs), and a diff deep in the sim import
    # graph stacks the expensive sim suites on top of it: 917 collected
    # files on PR #3342 against 782 on a comparable net-only PR, so the
    # floor plus about 135 sim-heavy files, which contiguous hash packing could
    # can concentrate in one shard. On 2026-08-12 shard 1 of PR #3342 was
    # bound-killed at a 20.25
    # minute wall on two attempts, both with the test step at 19.0 minutes
    # and still running (deterministic workload, not runner variance), and
    # fix/item-provenance-boundaries died the same way at 20.08; sibling
    # GREEN shards on the same run walked 16 to 17 minutes against the 13.6
    # prior baseline, so that runner set ran about 1.25x slow. 40 applied
    # release-gate's slow-runner arithmetic to this workload: the projected
    # completed shard 1 wall was about 22 minutes on those runners, about 18
    # fast-runner-equivalent, and 18 x 1.60 (the fast-to-slow runner ratio
    # measured at b160a1ba18) x 1.37 (the margin the original bounds were
    # chosen with) was 39.5. Label the base honestly when re-deriving: no
    # killed attempt ever completed, so 22 is an extrapolation bounded
    # below by the kill wall; if a healthy shard is bound-killed again,
    # remeasure a completed wall rather than re-raising by feel. The
    # stall-detection buy survives the raise: the
    # stall class dies in checkout or setup, where ci-stall-rerun.yml's
    # nothing-ran-after-it predicate reruns it regardless of the bound's
    # value, and the git low-speed abort still ends trickle transfers in
    # about 2 minutes; a healthy long test step, which both 2026-08-12
    # kills interrupted, is exactly what this bound must not kill. The
    # accepted cost is the hang variant the low-speed floor does not
    # police (the header's run 31402711619): a hung shard then burned 40
    # minutes before the reactor could fire, double the old latency, taken
    # because a false kill of a required healthy shard blocks PRs outright
    # while a hang costs only latency. The
    # queue-side ceiling this has to fit under is recorded in
    # docs/merge-queue.md.
    #
    # 2026-08-14 re-derivation. TWO MODES SHARE THIS ONE BOUND: selective
    # (every ordinary pull_request) and full (merge_group, pushes, widened
    # PRs), and selective is the expensive one, because it stacks the
    # always-run floor under the related set. The measurement trap that
    # briefly sized this at 26: any PR touching ci.yml widens to full by the
    # workflow-side trigger, so a bounds PR can never observe a selective
    # wall on its own run, and the two runs first cited as "the merged-leg
    # PR's selective run" (31761236945, 31763736783) were in fact full-mode
    # for exactly that reason. Label the MODE of every base when
    # re-deriving. Measured bases, by mode AND tree: worst healthy
    # SELECTIVE shard wall 16.55 minutes (run 31765273776 shard 4,
    # merged-leg pipeline, pre-sparse tree). NO post-sparse selective wall
    # has been observed yet (the sparse cone only trims checkout, so the
    # test step should match; the nearest comparator is a post-split
    # pre-sparse selective wall of 13.2, run 31770499114), and 16.55 stays
    # the conservative base until post-sparse selective walls exist. Worst
    # healthy FULL wall post-split is 11.5 (31768778440, 31774143931; a
    # pre-split tree walked 12.03 in 31752830175). 16.55 x 1.60 x 1.37 = 36.3,
    # so 37. On the reactor: it reruns only setup-phase kills, never a
    # test-step kill, and never fires on merge_group at all, so this bound
    # is the only thing that ends a hung test step, the rerun is manual,
    # and a false kill blocks a REQUIRED check outright, which is why the
    # base stays the conservative one. The packing rebalance that follow-up
    # asked for LANDED 2026-08-14 (measured-weight LPT via the balanced
    # sequencer; under sha1-contiguous packing shard 4 was the worst shard
    # in every sampled selective run). 37 deliberately survives the change:
    # re-derive only from healthy selective walls MEASURED on the new
    # packing, never from the simulation (the review round showed a
    # simulated 9.79m flat pack against a real 11.21m when one modeling
    # assumption was wrong).
    timeout-minutes: 37
    runs-on: ubuntu-latest

    # The real-SQL merge bar (ruling R16, recorded in the woc-marketplace
    # hardening state): every shard leg gets its own Postgres so the
    # floor-resident pg suites RUN here instead of skipping green on a
    # missing TEST_DATABASE_URL. One isolated instance per job leg holds the
    # suites' fixed-database-name contract (two concurrent batteries must
    # never share a server). The health check lives in the service options
    # so readiness needs no wait step; the password is a throwaway literal
    # for an ephemeral loopback container, not a secret. Port 5432 on
    # purpose: the vitest fallback DATABASE_URL names 5433 and must stay a
    # dead letter. ACCEPTED COST, recorded: services boot before the first
    # step regardless of the step gate, so docs-only and release-to-main
    # legs pay a container boot to no-op, and a registry outage can red a
    # leg that would have skipped green (the alternative, a separate
    # DB-only job, would mint new required-check names; see
    # docs/merge-queue.md before ever taking that trade).
    # WOCC_EXPECT_PG arms the runtime sentinel (tests/ci_pg_presence.test.ts)
    # on ANY runner brand: it travels in the same job-level env block the
    # source pin fingerprints, so losing this block disarms nothing quietly.
    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: wocc_ci
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres -d wocc_ci"
          --health-interval 5s
          --health-timeout 3s
          --health-retries 20
    env:
      TEST_DATABASE_URL: postgres://postgres:postgres@127.0.0.1:5432/wocc_ci
      WOCC_EXPECT_PG: '1'

    # fail-fast stays off: shards pass or fail independently, so one red shard
    # never cancels the others and a failure report always covers the whole
    # suite.
    strategy:
      fail-fast: false
      matrix:
        shard: [1, 2, 3, 4, 5, 6, 7, 8]

    steps:
      # The step gate (see the job comment): identical on every step of this
      # job, pinned that way by tests/ci_workflow.test.ts. A leg that this run
      # does not apply to (docs-only PR, release-to-main PR) skips every step
      # and reports green under its required suffixed name. The code arm stays
      # != 'false' so a missing or empty classifier output runs the suite,
      # never skips it (fail closed).
      - name: Check out repository
        if: (github.event_name != 'pull_request' || github.base_ref != 'main' || !startsWith(github.head_ref, 'release/')) && needs.changes.outputs.code != 'false'
        uses: actions/checkout@v6
        with:
          # Blobless sparse checkout (the sparse-checkout input implies
          # filter=blob:none in actions/checkout): docs/screenshots is
          # 794 MB of committed PR evidence the test jobs never read except
          # the referenced subtrees below, and the measured checkout pathology
          # (run 31741012359: lane A spent 11m21s fetching before an 8m25s
          # test step) scales with the blob payload. Non-cone patterns,
          # last match wins: everything in, screenshot DIRECTORIES out (the
          # trailing slash leaves root-level files in), every subtree the
          # repo actually references back in. The list is DERIVED and
          # COUPLED: tests/ci_workflow.test.ts rescrapes every tracked file
          # outside docs/screenshots (enumerated from the git index, so new
          # directories join the corpus automatically) and requires SET
          # EQUALITY with this list, so a reference this cone omits, or a
          # cone entry nothing references anymore, fails the pin in the same
          # change. pr-checks, browser-gate, release-checks, and
          # release-version-gate deliberately keep the full tree.
          sparse-checkout: |
            /*
            !/docs/screenshots/*/
            /docs/screenshots/admin-cheater-mark/
            /docs/screenshots/admin-guild-bank-panel/
            /docs/screenshots/aura-tracks/
            /docs/screenshots/bank-storage-charters/
            /docs/screenshots/bank-vault-tab/
            /docs/screenshots/cosmetics-window/
            /docs/screenshots/deed-border-cartouche/
            /docs/screenshots/eastbrook-grand-armoury/
            /docs/screenshots/eastbrook-vale-rebuild/
            /docs/screenshots/far-foliage-impostors/
            /docs/screenshots/fenbridge-rebuild/
            /docs/screenshots/guild-bank-history/
            /docs/screenshots/guild-bank-tab/
            /docs/screenshots/guild-pledge-board/
            /docs/screenshots/guild-social-v1/
            /docs/screenshots/harvest-button-refresh/
            /docs/screenshots/ignivar-raid/
            /docs/screenshots/ignivar-raid-expansion/
            /docs/screenshots/intentional-gathering-pr1/
            /docs/screenshots/intentional-gathering-pr2/
            /docs/screenshots/item-art-consistency-2026-08-09/
            /docs/screenshots/market-house-redesign/
            /docs/screenshots/masterwrought-art-completion-2026-09-02/
            /docs/screenshots/placeholder-art-completion-2026-08-09/
            /docs/screenshots/r35-admin-professions-inspector/
            /docs/screenshots/release-v036-skill-normalization-2026-08-10/
            /docs/screenshots/release-v039-icon-art-first-pass-2026-08-16/
            /docs/screenshots/target-dots/
            /docs/screenshots/touch-ui-rework/
            /docs/screenshots/vault-fine-mark/
            /docs/screenshots/wildheart/
            /docs/screenshots/woc-market/
          sparse-checkout-cone-mode: false
          # Explicit for the reader; the sparse-checkout input
          # already implies it (matching the changes job's step).
          filter: blob:none

      - name: Set up pnpm
        if: (github.event_name != 'pull_request' || github.base_ref != 'main' || !startsWith(github.head_ref, 'release/')) && needs.changes.outputs.code != 'false'
        uses: pnpm/action-setup@v4
        with:
          version: 10.34.5

      - name: Set up Node.js
        if: (github.event_name != 'pull_request' || github.base_ref != 'main' || !startsWith(github.head_ref, 'release/')) && needs.changes.outputs.code != 'false'
        uses: actions/setup-node@v6
        with:
          node-version: 26
          cache: pnpm

      - name: Install dependencies
        if: (github.event_name != 'pull_request' || github.base_ref != 'main' || !startsWith(github.head_ref, 'release/')) && needs.changes.outputs.code != 'false'
        run: pnpm install --frozen-lockfile

      # Persist the vitest transform cache across runs (vite.config.ts enables
      # experimental.fsModuleCache; the store is
      # node_modules/.experimental-vitest-cache). STALENESS safety lives inside
      # vitest, not in this key: every entry is keyed by sha1 over the module
      # source, the config file contents, NODE_ENV, and the fsModuleCache
      # format version, so a changed input can only miss (a vitest upgrade
      # rotates via the lockfile, in this key and in vitest's own integrity
      # discard, which hashes the derived node_modules/.pnpm/lock.yaml, the
      # byte-identical copy pnpm writes during install and therefore present
      # only after it, one reason this step sits after the install step).
      # TAMPER safety is GitHub's per-ref cache scoping, not vitest: entries
      # are keyed by content, never verified against it, and the store is
      # executed as module code, so this step must never gain restore-keys or
      # a shared scope without redoing that analysis. Accepted residual: a
      # fork PR's first run (which already executes the PR's own code by
      # design) can seed its own refs/pull/N/merge scope for later runs of
      # the SAME PR; that scope cannot reach other PRs, merge_group runs,
      # release-gate, or the nightly, and the PR tier holds no secrets and a
      # read-only token. The key pins every node_modules-layout input
      # (lockfile, vite config, .npmrc for node-linker, package.json for the
      # packageManager pin) so a re-layout starts clean instead of restoring
      # a store vitest would discard or mis-resolve; no restore-keys for the
      # same reason. actions/cache saves only when the primary key missed, so
      # entries stay bounded: one 57-73 MB compressed entry per shard per
      # toolchain state per scope (measured 2026-08-06), written once when
      # the key is new and never refreshed within that state, so the hit rate
      # decays as sources drift until the next rotation (start there if the
      # speedup fades). The binding budget is GitHub's 10 GB per-repo cache
      # quota with LRU eviction: about 0.5 GB per toolchain state per scope
      # (8 entries at 57-73 MB; no single scope holds both matrices: on the
      # one event both jobs now see, a release-to-main PR, pr-gate's copy of
      # this step is gated off by the step gate above, so release-gate's
      # matrix is the only producer on that ref; the separation lives in the
      # STEP gate, not in the job routing), roughly 1 GB across the two
      # long-lived producer scopes (main
      # plus the release branch) beside the pnpm store, Playwright, and tsc
      # caches. If hot-entry eviction is ever observed, the lever is
      # restricting this step's save to push events, never restore-keys
      # (though note the long-sims lane leans on PR-scope saves, so that
      # lever costs the lane most of its benefit). Scope arithmetic after
      # the lane copies landed: a pr-gate-tier producer scope (main, dev-*)
      # holds 10 entries (8 shard + 2 lane halves); the release scope stays
      # at 8 (release-gate is deliberately not lane-split).
      # Producers beyond release-gate: pr-gate's own main and dev-* push runs
      # seed those branches' scopes the same way.
      # Per-shard keys: in full mode each shard transforms
      # its own pack's import closure; in selective mode the collected set is
      # smaller and only partially resembles the full-mode store it restores,
      # which still hits on the shared source closure that dominates the
      # store. release-gate carries the same step with the same key: its
      # release/** push runs are what save the store into the base-branch
      # scope PR runs can restore from (pr-gate never runs on release pushes,
      # and a PR-scoped save is invisible to every other PR). Both selective
      # legs and the full replay read the same store: every leg is vitest
      # under the same config.
      - name: Cache vitest transform cache
        if: (github.event_name != 'pull_request' || github.base_ref != 'main' || !startsWith(github.head_ref, 'release/')) && needs.changes.outputs.code != 'false'
        uses: actions/cache@v4
        with:
          path: node_modules/.experimental-vitest-cache
          key: vitest-fsmodule-${{ runner.os }}-shard${{ matrix.shard }}-${{ hashFiles('pnpm-lock.yaml', 'vite.config.ts', '.npmrc', 'package.json') }}

      # The shard entry regenerates the artifacts itself, once per job, BY
      # DESIGN (the S3 guard, guide freshness, and the git-subprocess suites
      # need the generated artifacts no matter which shard they hash into; the
      # merged selective leg is a bare vitest-related invocation with no npm
      # lifecycle, so scripts/ci_shard_test.mjs runs pretest before spawning
      # and the npm-test legs skip their lifecycle copy). The freshness diff
      # lives in pr-checks. Worker count resolves inside the script: the
      # half-cores measured default unless a step env sets a valid
      # WOC_TEST_WORKERS. None is set: both alternatives were measured and
      # regressed (4 workers, run 31107474546; 3 workers, run 31771637461,
      # three unrelated default-timeout blowouts for at most a minute of
      # wall). The ruling lives in scripts/ci_shard_test.mjs.
      #
      # Phase 2 (docs/qa-gate.md, "Selective PR-tier CI"): the runner reads the
      # changes job's selection decision. test_mode=full spawns the old step
      # (`npm test -- --shard=i/8`) minus the long-sims lane files, which the
      # pr-long-sims-a/-b jobs run in this same run (Phase 4); test_mode=selective
      # runs ONE merged sharded vitest-related leg: the changed sources plus
      # the always-run floor (blind/partial tests + invariant guard suites +
      # changed tests, recomputed in this tree, minus the lane's floor
      # members) as self-selecting seeds. Decisions and skip counts print in
      # the job log.
      # Release-gate below keeps the unconditional full run line; the nightly
      # workflow re-proves the full suite daily (docs/qa-gate.md).
      - name: Run tests (PR tier, shard ${{ matrix.shard }} of 8)
        if: (github.event_name != 'pull_request' || github.base_ref != 'main' || !startsWith(github.head_ref, 'release/')) && needs.changes.outputs.code != 'false'
        env:
          TEST_MODE: ${{ needs.changes.outputs.test_mode }}
          TEST_MODE_REASON: ${{ needs.changes.outputs.test_mode_reason }}
          CHANGED_FILES: ${{ needs.changes.outputs.changed_files }}
        run: node scripts/ci_shard_test.mjs --shard=${{ matrix.shard }}/8

  # The long-sims lanes (Phase 4 of the CI/CD performance packet; split into
  # two parallel jobs by the lane-diet PR). The pair runs the CI_LONG_SUITES
  # files (scripts/lib/ci_shard_plan.mjs) that every pr-gate shard leg
  # excludes, one CI_LONG_SUITE_HALVES half per job, so the lane's wall clock
  # is the slower half instead of the whole list. Same event
  # routing and code path filter as pr-gate, so each job reports (or
  # docs-only-skips) on every pull_request AND merge_group run, which is the
  # bar docs/merge-queue.md sets for a REQUIRED check; both jobs are required
  # (they carry coverage the shards deliberately exclude), so their display
  # names are load-bearing three ways (ruleset, tests/ci_workflow.test.ts,
  # docs/merge-queue.md). In selective mode each lane runs only its half's
  # lane files the floor or the diff would have carried; in full mode and on
  # any unprovable input it runs its whole half, mirroring the shard
  # planner's fail-closed ladder, so mode for mode the shards plus the two
  # lanes run exactly what the pre-lane shard plan would have run.
  # release-gate is deliberately NOT lane-split: release/** pushes keep the
  # full suite in their 8 shards.
  pr-long-sims-a:
    name: PR long sims A
    needs: changes
    if: ((github.event_name == 'pull_request' && (github.base_ref != 'main' || !startsWith(github.head_ref, 'release/'))) || (github.event_name == 'push' && !startsWith(github.ref, 'refs/heads/release/')) || github.event_name == 'workflow_dispatch' || github.event_name == 'merge_group') && needs.changes.outputs.code != 'false'
    # Each lane half concentrates slow suites in one required job with no
    # sibling shard to report around it, so a hang would otherwise hold a
    # required check for GitHub's 6 hour default. The single-job lane's bound
    # was 60 (run 31290316610 measured ~4400s aggregate suite time on a
    # slow-quartile runner). The lane-diet PR then cut the nine suites'
    # aggregate from 1786s to 901s LOCAL-measured and split the list (half A
    # 403s / half B 498s aggregate; per-suite evidence in the PR body), and
    # sized 20 from a projection of under 10 minutes per half.
    #
    # That projection did not survive CI measurement, and this comment is the
    # re-derivation the lane-diet note asks for whenever a member's cost moves.
    # On run 31450179645 (2026-08-11, a healthy runner, full mode) lane A ran
    # a 13.73 minute JOB wall and lane B 12.55, because owned_class_balance_harness
    # alone measured 739268ms in-lane: the local aggregate for all three half-A
    # files understates that ONE file by most of a factor of two. Lane A's wall
    # breaks down as 66s of setup plus a 753s test step, so the job and step
    # figures are close here only because its caches were warm; size from the
    # JOB wall regardless, because a setup spike counts inside the bound.
    # Lane isolation does not reliably shrink the harness either: in-lane it
    # measured 739s, which sits BETWEEN the two in-shard figures (527s fast,
    # 842s slow). Those three samples are not controlled against each other,
    # so read them as runner-to-runner variance dominating, not as a proof
    # about contention.
    #
    # 2026-08-13: the owned-class harness pair was split into the
    # owned_class_balance_* / owned_class_raid_* single-responsibility files
    # and the halves were re-balanced, so no lane chain carries that single
    # 13-minute file any more. The figures above stay as the measured record;
    # the 2026-08-14 re-derivation below is the follow-up change that note
    # promised.
    #
    # 30 applied the same arithmetic as release-gate's bound to the pre-split
    # walls: healthy JOB wall times the 1.60 fast-to-slow runner ratio
    # measured at b160a1ba18 (13.73 x 1.60 = 22.0 for the slower half), then
    # the 1.37x margin the original bounds were chosen with. At 20 this
    # REQUIRED check was roughly one slow runner away from bound-killing and
    # blocking the merge queue, which is the more expensive failure than the
    # release backstop's.
    #
    # 2026-08-14 re-derivation, from post-rebalance healthy JOB walls: across
    # the full-mode green runs since the second-tier split and lane rebalance
    # (31768778440, 31774143931), the worst lane wall is lane B at 12.5
    # minutes. 12.5 x 1.60 x 1.37 = 27.4, so 28. Both halves share one bound
    # so the a/b assignment can still rebalance freely without re-sizing. The
    # queue-side ceiling this has to fit under is recorded in
    # docs/merge-queue.md.
    timeout-minutes: 28
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v6
        with:
          # Same blobless sparse cone as pr-gate's checkout (rationale and
          # the reader coupling live there); pinned identical by
          # tests/ci_workflow.test.ts.
          sparse-checkout: |
            /*
            !/docs/screenshots/*/
            /docs/screenshots/admin-cheater-mark/
            /docs/screenshots/admin-guild-bank-panel/
            /docs/screenshots/aura-tracks/
            /docs/screenshots/bank-storage-charters/
            /docs/screenshots/bank-vault-tab/
            /docs/screenshots/cosmetics-window/
            /docs/screenshots/deed-border-cartouche/
            /docs/screenshots/eastbrook-grand-armoury/
            /docs/screenshots/eastbrook-vale-rebuild/
            /docs/screenshots/far-foliage-impostors/
            /docs/screenshots/fenbridge-rebuild/
            /docs/screenshots/guild-bank-history/
            /docs/screenshots/guild-bank-tab/
            /docs/screenshots/guild-pledge-board/
            /docs/screenshots/guild-social-v1/
            /docs/screenshots/harvest-button-refresh/
            /docs/screenshots/ignivar-raid/
            /docs/screenshots/ignivar-raid-expansion/
            /docs/screenshots/intentional-gathering-pr1/
            /docs/screenshots/intentional-gathering-pr2/
            /docs/screenshots/item-art-consistency-2026-08-09/
            /docs/screenshots/market-house-redesign/
            /docs/screenshots/masterwrought-art-completion-2026-09-02/
            /docs/screenshots/placeholder-art-completion-2026-08-09/
            /docs/screenshots/r35-admin-professions-inspector/
            /docs/screenshots/release-v036-skill-normalization-2026-08-10/
            /docs/screenshots/release-v039-icon-art-first-pass-2026-08-16/
            /docs/screenshots/target-dots/
            /docs/screenshots/touch-ui-rework/
            /docs/screenshots/vault-fine-mark/
            /docs/screenshots/wildheart/
            /docs/screenshots/woc-market/
          sparse-checkout-cone-mode: false
          # Explicit for the reader; the sparse-checkout input
          # already implies it (matching the changes job's step).
          filter: blob:none

      - name: Set up pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.34.5

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 26
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      # Same vitest transform cache as the shard matrices (the Phase 4 rider:
      # whichever of the cache step and the lane landed second gains the
      # other). The full rationale (staleness vs tamper safety, layout-pinned
      # key, no restore-keys, save-on-miss-only, measured sizes) sits on the
      # pr-gate copy of this step. This job has no shard matrix, so the key
      # carries a lane segment where the matrices carry shard${{ matrix.shard }}:
      # a bare matrix.shard would render empty here, and each lane half's
      # store (in full mode, its half of the CI_LONG_SUITES import closures;
      # in selective mode only the floor/changed members, so a PR-scope save
      # can be a thin store that save-on-miss never refreshes within that
      # key) only partially resembles any one shard's or the other half's,
      # so it earns its own entry instead of borrowing keys. Producer
      # reality for this key: release-gate is deliberately not lane-split,
      # so release/** push runs never save a lane entry into the base-branch
      # scope; main's entry (seeded by this job's own main and dev-* push
      # runs) only hits while the release branch's four key inputs still
      # hash like main's, which they do not whenever main trails the release
      # lockfile (true today), so for release-based PRs most warm hits come
      # from later runs of the SAME PR restoring the first run's save into
      # the PR's own scope.
      - name: Cache vitest transform cache
        uses: actions/cache@v4
        with:
          path: node_modules/.experimental-vitest-cache
          key: vitest-fsmodule-${{ runner.os }}-lane-long-sims-a-${{ hashFiles('pnpm-lock.yaml', 'vite.config.ts', '.npmrc', 'package.json') }}

      # Same selection relay as the shard step (env, never run-line
      # interpolation); the lane entry regenerates the i18n artifacts once
      # before spawning, exactly as in every shard (its npm-test legs skip
      # their lifecycle copy so nothing regenerates twice).
      - name: Run tests (PR tier, long-sims lane A)
        env:
          TEST_MODE: ${{ needs.changes.outputs.test_mode }}
          TEST_MODE_REASON: ${{ needs.changes.outputs.test_mode_reason }}
          CHANGED_FILES: ${{ needs.changes.outputs.changed_files }}
        run: node scripts/ci_shard_test.mjs --lane=long-sims-a

  # Second half of the long-sims lane pair; every design note sits on
  # pr-long-sims-a above (identical shape, its own CI_LONG_SUITE_HALVES half
  # and its own cache key segment).
  pr-long-sims-b:
    name: PR long sims B
    needs: changes
    if: ((github.event_name == 'pull_request' && (github.base_ref != 'main' || !startsWith(github.head_ref, 'release/'))) || (github.event_name == 'push' && !startsWith(github.ref, 'refs/heads/release/')) || github.event_name == 'workflow_dispatch' || github.event_name == 'merge_group') && needs.changes.outputs.code != 'false'
    # Sized with pr-long-sims-a (evidence there, in the lane-diet PR, and in
    # the dated re-derivations the sibling comment carries).
    timeout-minutes: 28
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v6
        with:
          # Same blobless sparse cone as pr-gate's checkout (rationale and
          # the reader coupling live there); pinned identical by
          # tests/ci_workflow.test.ts.
          sparse-checkout: |
            /*
            !/docs/screenshots/*/
            /docs/screenshots/admin-cheater-mark/
            /docs/screenshots/admin-guild-bank-panel/
            /docs/screenshots/aura-tracks/
            /docs/screenshots/bank-storage-charters/
            /docs/screenshots/bank-vault-tab/
            /docs/screenshots/cosmetics-window/
            /docs/screenshots/deed-border-cartouche/
            /docs/screenshots/eastbrook-grand-armoury/
            /docs/screenshots/eastbrook-vale-rebuild/
            /docs/screenshots/far-foliage-impostors/
            /docs/screenshots/fenbridge-rebuild/
            /docs/screenshots/guild-bank-history/
            /docs/screenshots/guild-bank-tab/
            /docs/screenshots/guild-pledge-board/
            /docs/screenshots/guild-social-v1/
            /docs/screenshots/harvest-button-refresh/
            /docs/screenshots/ignivar-raid/
            /docs/screenshots/ignivar-raid-expansion/
            /docs/screenshots/intentional-gathering-pr1/
            /docs/screenshots/intentional-gathering-pr2/
            /docs/screenshots/item-art-consistency-2026-08-09/
            /docs/screenshots/market-house-redesign/
            /docs/screenshots/masterwrought-art-completion-2026-09-02/
            /docs/screenshots/placeholder-art-completion-2026-08-09/
            /docs/screenshots/r35-admin-professions-inspector/
            /docs/screenshots/release-v036-skill-normalization-2026-08-10/
            /docs/screenshots/release-v039-icon-art-first-pass-2026-08-16/
            /docs/screenshots/target-dots/
            /docs/screenshots/touch-ui-rework/
            /docs/screenshots/vault-fine-mark/
            /docs/screenshots/wildheart/
            /docs/screenshots/woc-market/
          sparse-checkout-cone-mode: false
          # Explicit for the reader; the sparse-checkout input
          # already implies it (matching the changes job's step).
          filter: blob:none

      - name: Set up pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.34.5

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 26
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      # Same vitest transform cache step as pr-long-sims-a, keyed for this
      # half (rationale on the pr-gate copy).
      - name: Cache vitest transform cache
        uses: actions/cache@v4
        with:
          path: node_modules/.experimental-vitest-cache
          key: vitest-fsmodule-${{ runner.os }}-lane-long-sims-b-${{ hashFiles('pnpm-lock.yaml', 'vite.config.ts', '.npmrc', 'package.json') }}

      # Same selection relay as the shard step (env, never run-line
      # interpolation); the lane entry regenerates the i18n artifacts once
      # before spawning, exactly as in every shard (its npm-test legs skip
      # their lifecycle copy so nothing regenerates twice).
      - name: Run tests (PR tier, long-sims lane B)
        env:
          TEST_MODE: ${{ needs.changes.outputs.test_mode }}
          TEST_MODE_REASON: ${{ needs.changes.outputs.test_mode_reason }}
          CHANGED_FILES: ${{ needs.changes.outputs.changed_files }}
        run: node scripts/ci_shard_test.mjs --lane=long-sims-b

  # Runs in parallel with pr-gate: same if-condition, no needs edge in either
  # direction. The default pull_request merge-ref checkout MUST be preserved
  # here: the freshness diff checks the MERGE RESULT against the base branch,
  # not just the PR head.
  pr-checks:
    name: PR checks
    # Same composition as pr-gate: release exclusion + code path filter. Skipped
    # entirely on docs-only PRs (malware/typecheck/builds are code-path gates).
    # merge_group lands here too: the queue re-proves freshness, typecheck, and
    # every build on the merge result, not just the PR head.
    needs: changes
    if: ((github.event_name == 'pull_request' && (github.base_ref != 'main' || !startsWith(github.head_ref, 'release/'))) || (github.event_name == 'push' && !startsWith(github.ref, 'refs/heads/release/')) || github.event_name == 'workflow_dispatch' || github.event_name == 'merge_group') && needs.changes.outputs.code != 'false'
    # Unmatrixed single toolchain-setup-plus-checks job, comparable in shape to
    # browser-gate, but heavier: i18n generation, the malware gate, a
    # typecheck, and four builds. 20 was sized beside the shard matrices'
    # original bound and still fits this job's serialized check list;
    # pr-gate's own bound has since moved for its sim-heavy selective test
    # packs, which this job does not run, so the two values parted on
    # purpose. A stalled runner here still dies and reruns instead of
    # holding a required check for hours.
    timeout-minutes: 20
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Set up pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.34.5

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 26
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Generate i18n artifacts
        run: npm run i18n:gen

      # Post the coverage counts of the gitignored i18n.status.summary.json (written
      # by the i18n:gen step above) to the job summary as the out-of-band audit trail.
      - name: Post i18n coverage summary
        run: node scripts/i18n_coverage_summary.mjs

      # Fast, legible fail BEFORE the long build steps if a committed generated
      # artifact drifted from source (someone changed an overlay or a catalog key but
      # forgot to regenerate). The status registry and its counts summary are both
      # gitignored; the diff covers the committed line-item locale slices AND the
      # generated TranslationKey union. The test suite also enforces this, this just
      # surfaces it sooner.
      - name: Verify committed i18n artifacts are fresh
        run: git diff --exit-code -- src/ui/i18n.resolved.generated src/admin/i18n.resolved.generated src/ui/i18n.catalog/translation_keys.generated.ts

      # Release-gate malicious-code scan. The flagger is high-recall (most hits are
      # expected dev-tooling false positives), so this uses --gate: it fails only on
      # a HIGH-severity signature that survives the path-aware priors (a wallet
      # drainer, a hardcoded-credential auth bypass, an exfil channel, a planted
      # instruction-file injection). tests/malware_scan.test.ts asserts the same
      # inside `npm test`; this just surfaces it sooner and more legibly.
      - name: Malicious-code gate
        run: npm run security:gate

      # tsc's own incremental cache (node_modules/.cache/tsc/buildinfo.json, via
      # check:ts's --incremental flag). Keyed on the lockfile and tsconfig so a
      # dependency or compiler-option change starts clean; tsc still verifies
      # every file's content hash against the cache, so a stale restore only
      # saves the unchanged-file re-check, never hides a real type error.
      - name: Cache tsc incremental buildinfo
        uses: actions/cache@v4
        with:
          path: node_modules/.cache/tsc
          key: tsc-buildinfo-${{ runner.os }}-${{ hashFiles('pnpm-lock.yaml', 'tsconfig.json') }}-${{ github.sha }}
          restore-keys: |
            tsc-buildinfo-${{ runner.os }}-${{ hashFiles('pnpm-lock.yaml', 'tsconfig.json') }}-

      # Same turbo multi-task run gate.mjs uses for wall-clock overlap across
      # these four independent pure steps (scripts/lib/gate_steps.mjs), so CI and
      # the local gate share one cache-aware idiom instead of drifting apart.
      # The Discord bot bundles from the same tree and ships in the same image
      # (Dockerfile runs build:bot), so a broken bot bundle must fail here rather
      # than on the production host during docker compose up --build.
      - name: Typecheck + env/server/bot builds
        run: npx turbo run check:types build:env build:server build:bot --ui=stream

      - name: Build client
        run: npm run wiki:content && npm run build:bundle

      # The client build just regenerated all three committed build manifests
      # (wiki:content writes the guide content, build:bundle's pregen writes
      # the SFX and media manifests), so trackedness plus the diff prove the
      # committed copies against this tree's own sources. The selective planner treats
      # exactly these paths as inert-but-fed-to-related ONLY on the strength
      # of this step (GENERATED_MANIFEST_ARTIFACT_FILES in
      # scripts/lib/gate_select_plan.mjs; tests/ci_workflow.test.ts welds the
      # classifier list, this argv, and the local gate list to each other).
      - name: Manifest freshness (committed build manifests)
        run: git ls-files --error-unmatch -- src/game/sfx_manifest.generated.ts src/guide/content.generated.ts src/render/assets/manifest.generated.ts public/audio/sfx/runtime-pack.json scripts/sfx/sfx_gain_ceiling.generated.json && git diff --exit-code -- src/game/sfx_manifest.generated.ts src/guide/content.generated.ts src/render/assets/manifest.generated.ts public/audio/sfx/runtime-pack.json scripts/sfx/sfx_gain_ceiling.generated.json

  # A release ref runs THREE parallel jobs: release-gate (this one) runs the test
  # suite at PR tier, release-i18n runs the release TIER over the suites that read
  # the flag, and release-checks runs the serialized checks, so the release critical
  # path is max(tests, tier, checks) instead of tests-then-builds on shard 1.
  # Locally `npm run gate` (scripts/gate.mjs) still runs the SAME combined step
  # list serially with ONE full unsharded vitest by design, plus the same dedicated
  # release-tier step; keep that script's step list in sync when adding or removing
  # a step in any of the three jobs.
  release-gate:
    # NOT "21-locale": since #2820 the 21-locale assertions live in release-i18n, and
    # the label is what an operator reads in the checks list to tell the two reds apart.
    name: Release tests
    if: (github.event_name == 'pull_request' && github.base_ref == 'main' && startsWith(github.head_ref, 'release/')) || (github.event_name == 'push' && startsWith(github.ref, 'refs/heads/release/'))
    # Sized from this matrix's own workload, independently of pr-gate's
    # bound: this matrix keeps the long sims in-shard by design, since
    # pr-gate hands its CI_LONG_SUITES files to
    # the lane pair, and release-gate deliberately does not
    # (scripts/lib/ci_shard_plan.mjs states that ruling), so a release shard
    # can draw several multi-minute harnesses at once. Since 2026-08-14 the
    # balanced sequencer (vite.config.ts) packs shards by MEASURED durations,
    # which spreads those harnesses; the bound predates that and stays sized
    # for the contiguous worst until a measured release wall justifies
    # re-deriving it, because sizing for the healthy median is what made it
    # fragile before.
    #
    # 20 was sized from a 14.63 minute healthy worst case (run 31117143257).
    # That premise died on 2026-08-11 at b160a1ba18, where shard 1 drew FOUR
    # of the then-nine long suites (owned_class_balance_harness, nythraxis_matrix,
    # hunter_dps_balance, and druid_balance_probe) and was bound-killed four
    # times (run 31443047694 attempts 1 through 3, run 31443040285 attempt 1)
    # while attempt 2 of that same push run passed the identical scope in 16
    # minutes. Same commit, same warm transform cache: the differentiator was
    # runner speed, with owned_class_balance_harness measured at 527s on the
    # fast runner and 842s on the slow one, a ratio of 1.60. (2026-08-13:
    # that harness pair has since been split into the owned_class_balance_* /
    # owned_class_raid_* files, so one shard draw can no longer carry a
    # 13-minute single file; the figures stay as the record behind the 1.60
    # ratio.)
    #
    # Sizing, stated as the arithmetic rather than a feel. Every figure below
    # is a JOB wall, which is what timeout-minutes actually polices: setup
    # (checkout, pnpm, node, install, cache restore) counts inside the bound
    # and can spike on its own, so a base taken from the test STEP alone would
    # under-size this. Label the kind whenever you re-derive it. The healthy
    # job wall is the 16 minute passing attempt, so the slow-runner projection
    # is 16 x 1.60 = 25.6 minutes, and 35 holds the 1.37x margin the 20 bound
    # was originally chosen with (20 / 14.63). Scaling the whole job wall by a
    # CPU ratio is deliberately conservative, since setup is I/O bound and does
    # not scale with it. Do NOT re-derive this from the
    # killed attempt's file count: it had finished 271 of 330 files at 18m53s,
    # but druid_balance_probe was still unfinished, so the remaining time was
    # bounded below by a multi-minute file rather than by a per-file average,
    # and the linear extrapolation that suggests (about 23 minutes) reads low.
    #
    # Raising this costs stall-kill latency on this job only, which is the
    # right trade for a post-merge backstop. Note what does NOT cover it: the
    # stall reactor (.github/workflows/ci-stall-rerun.yml) reruns a bound-killed
    # SETUP step or a failed checkout, never a test-step kill like this one, and
    # it stays inert until its workflow file reaches the default branch. A
    # test-step kill here is a manual rerun by design. pr-gate's bound is
    # derived separately from its own selective-pack evidence (on its bound
    # above); its shards exclude these suites, so this evidence does not
    # size it. The nightly workflow deliberately keeps its own
    # generous bounds and is untouched.
    #
    # 2026-08-14 re-derivation, from completed post-split walls: release-gate
    # runs on every release/** push, and the healthy worsts across those runs
    # are 15.00 to 16.43 minutes (31757915533 at 16.43, 31774886593 at
    # 16.28, 31771748136 at 16.22, 31764380309, 31769545742; the 19.03 wall
    # in 31746779792 shard 3 is a 9.6 minute CHECKOUT STALL over a 9.1
    # minute test step, the class the bound exists to kill, so it is
    # excluded from the base exactly like the lane stalls). 16.43 x 1.60 x
    # 1.37 = 36.0, so 36: the same arithmetic that first produced 35, one
    # minute looser now that the base is measured rather than projected.
    timeout-minutes: 36
    runs-on: ubuntu-latest

    # Same real-SQL wiring as pr-gate (ruling R16): a per-leg Postgres and
    # TEST_DATABASE_URL so the pg suites run at the release bar too.
    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: wocc_ci
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres -d wocc_ci"
          --health-interval 5s
          --health-timeout 3s
          --health-retries 20
    env:
      TEST_DATABASE_URL: postgres://postgres:postgres@127.0.0.1:5432/wocc_ci
      WOCC_EXPECT_PG: '1'
      # The mediawiki seed's release tier (masterwrought Phase 18): the seed is a
      # first-boot deploy artifact that trails content BY DESIGN through a cycle,
      # so the PR tier only guards its envelope and its dash-freedom, and the
      # byte-equality demand rides here, where a stale artifact is a real defect.
      # Its own flag, never I18N_RELEASE_TIER: tests/release_i18n_tier_coverage.test.ts
      # pins that flag's suite set three ways and would red on contact.
      MEDIAWIKI_SEED_RELEASE_TIER: '1'

    # No I18N_RELEASE_TIER here BY DESIGN (D8 revised, issue #2820). This job runs
    # the suite at PR tier so a red shard always means a real regression. The
    # release tier lives in the release-i18n job below, over the tier-sensitive
    # suites only, so "the locale fill has not run yet" can never mask a broken
    # test the way it did when one job carried both signals.

    # Same 8-shard matrix as pr-gate. fail-fast stays off so shards report
    # independently. Serialized checks live in release-checks, not here.
    strategy:
      fail-fast: false
      matrix:
        shard: [1, 2, 3, 4, 5, 6, 7, 8]

    steps:
      - name: Check out repository
        uses: actions/checkout@v6
        with:
          # Same blobless sparse cone as pr-gate's checkout (rationale and
          # the reader coupling live there); pinned identical by
          # tests/ci_workflow.test.ts.
          sparse-checkout: |
            /*
            !/docs/screenshots/*/
            /docs/screenshots/admin-cheater-mark/
            /docs/screenshots/admin-guild-bank-panel/
            /docs/screenshots/aura-tracks/
            /docs/screenshots/bank-storage-charters/
            /docs/screenshots/bank-vault-tab/
            /docs/screenshots/cosmetics-window/
            /docs/screenshots/deed-border-cartouche/
            /docs/screenshots/eastbrook-grand-armoury/
            /docs/screenshots/eastbrook-vale-rebuild/
            /docs/screenshots/far-foliage-impostors/
            /docs/screenshots/fenbridge-rebuild/
            /docs/screenshots/guild-bank-history/
            /docs/screenshots/guild-bank-tab/
            /docs/screenshots/guild-pledge-board/
            /docs/screenshots/guild-social-v1/
            /docs/screenshots/harvest-button-refresh/
            /docs/screenshots/ignivar-raid/
            /docs/screenshots/ignivar-raid-expansion/
            /docs/screenshots/intentional-gathering-pr1/
            /docs/screenshots/intentional-gathering-pr2/
            /docs/screenshots/item-art-consistency-2026-08-09/
            /docs/screenshots/market-house-redesign/
            /docs/screenshots/masterwrought-art-completion-2026-09-02/
            /docs/screenshots/placeholder-art-completion-2026-08-09/
            /docs/screenshots/r35-admin-professions-inspector/
            /docs/screenshots/release-v036-skill-normalization-2026-08-10/
            /docs/screenshots/release-v039-icon-art-first-pass-2026-08-16/
            /docs/screenshots/target-dots/
            /docs/screenshots/touch-ui-rework/
            /docs/screenshots/vault-fine-mark/
            /docs/screenshots/wildheart/
            /docs/screenshots/woc-market/
          sparse-checkout-cone-mode: false
          # Explicit for the reader; the sparse-checkout input
          # already implies it (matching the changes job's step).
          filter: blob:none

      - name: Set up pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.34.5

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 26
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      # Same vitest transform cache as pr-gate, same key on purpose: this
      # job's release/** PUSH runs are the producer that saves the store into
      # the release branch's scope, the base-branch scope PR runs restore
      # from (this job's other arm, the release-to-main pull_request, saves
      # into that PR's own scope like any PR run; pr-gate's main and dev-*
      # push runs seed those branches' scopes likewise). The full rationale
      # (staleness vs tamper safety, layout-pinned key, no restore-keys,
      # save-on-miss-only, measured sizes) sits on the pr-gate copy of this
      # step.
      - name: Cache vitest transform cache
        uses: actions/cache@v4
        with:
          path: node_modules/.experimental-vitest-cache
          key: vitest-fsmodule-${{ runner.os }}-shard${{ matrix.shard }}-${{ hashFiles('pnpm-lock.yaml', 'vite.config.ts', '.npmrc', 'package.json') }}

      # `npm test` regenerates the i18n artifacts itself (pretest) in EVERY
      # shard BY DESIGN (the S3 guard, guide freshness, and the git-subprocess
      # suites need the generated artifacts no matter which shard the balanced
      # sequencer assigns them to), so this job needs no explicit i18n:gen step;
      # the freshness diff lives in release-checks.
      # Full Sim suites are CPU-heavy. Keep each shard at half the runner's
      # available cores, matching the bounded local gate.
      - name: Run tests (PR tier, shard ${{ matrix.shard }} of 8)
        run: npm test -- --shard=${{ matrix.shard }}/8 --maxWorkers="$(node -p 'Math.max(1, Math.floor(require("node:os").availableParallelism() / 2))')"

  # The release tier, alone (issue #2820). Only the suites whose assertions change
  # under the flag run here, so this job answers exactly one question: has the
  # 21-locale fill been done? It is EXPECTED to be red for most of a cycle, and that
  # is the point: it is red on its own name, next to a release-gate that stays green
  # unless code is genuinely broken. Unsharded and seconds long (six files), so it
  # costs a runner slot, not runner minutes. The file list is pinned three ways
  # (this command, I18N_RELEASE_TIER_SUITES in scripts/lib/gate_steps.mjs, and a scan
  # of tests/ for readers of the flag) by tests/release_i18n_tier_coverage.test.ts,
  # so a new tier-sensitive suite cannot silently stop running under the tier.
  release-i18n:
    name: Release i18n
    if: (github.event_name == 'pull_request' && github.base_ref == 'main' && startsWith(github.head_ref, 'release/')) || (github.event_name == 'push' && startsWith(github.ref, 'refs/heads/release/'))
    # Unsharded and seconds long by design (five test files, see the job
    # comment above): the toolchain setup dominates the wall time. 10 mirrors
    # release-version-gate's bound for the same shape.
    timeout-minutes: 10
    runs-on: ubuntu-latest

    env:
      I18N_RELEASE_TIER: '1'

    steps:
      - name: Check out repository
        uses: actions/checkout@v6
        with:
          # Same blobless sparse cone as pr-gate's checkout (rationale and
          # the reader coupling live there): the release-tier i18n suites
          # read locale sources and docs/i18n prose, none of it under
          # docs/screenshots; pinned identical by tests/ci_workflow.test.ts.
          sparse-checkout: |
            /*
            !/docs/screenshots/*/
            /docs/screenshots/admin-cheater-mark/
            /docs/screenshots/admin-guild-bank-panel/
            /docs/screenshots/aura-tracks/
            /docs/screenshots/bank-storage-charters/
            /docs/screenshots/bank-vault-tab/
            /docs/screenshots/cosmetics-window/
            /docs/screenshots/deed-border-cartouche/
            /docs/screenshots/eastbrook-grand-armoury/
            /docs/screenshots/eastbrook-vale-rebuild/
            /docs/screenshots/far-foliage-impostors/
            /docs/screenshots/fenbridge-rebuild/
            /docs/screenshots/guild-bank-history/
            /docs/screenshots/guild-bank-tab/
            /docs/screenshots/guild-pledge-board/
            /docs/screenshots/guild-social-v1/
            /docs/screenshots/harvest-button-refresh/
            /docs/screenshots/ignivar-raid/
            /docs/screenshots/ignivar-raid-expansion/
            /docs/screenshots/intentional-gathering-pr1/
            /docs/screenshots/intentional-gathering-pr2/
            /docs/screenshots/item-art-consistency-2026-08-09/
            /docs/screenshots/market-house-redesign/
            /docs/screenshots/masterwrought-art-completion-2026-09-02/
            /docs/screenshots/placeholder-art-completion-2026-08-09/
            /docs/screenshots/r35-admin-professions-inspector/
            /docs/screenshots/release-v036-skill-normalization-2026-08-10/
            /docs/screenshots/release-v039-icon-art-first-pass-2026-08-16/
            /docs/screenshots/target-dots/
            /docs/screenshots/touch-ui-rework/
            /docs/screenshots/vault-fine-mark/
            /docs/screenshots/wildheart/
            /docs/screenshots/woc-market/
          sparse-checkout-cone-mode: false
          filter: blob:none

      - name: Set up pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.34.5

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 26
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      # `npm test` regenerates the i18n artifacts itself (pretest), which these
      # suites read.
      - name: Run i18n release-tier suites (21-locale + empty-pending)
        run: npm test -- tests/deed_i18n.test.ts tests/i18n_status_registry.test.ts tests/i18n_t_behavior.test.ts tests/localization_coverage.test.ts tests/localization_fixes.test.ts tests/reliquary_i18n.test.ts

  # Runs in parallel with release-gate: same if-condition, no needs edge in
  # either direction. Does not set I18N_RELEASE_TIER; the checks here do not
  # depend on it, and the release-i18n job alone owns the tier flag.
  release-checks:
    name: Release checks
    if: (github.event_name == 'pull_request' && github.base_ref == 'main' && startsWith(github.head_ref, 'release/')) || (github.event_name == 'push' && startsWith(github.ref, 'refs/heads/release/'))
    # Mirrors pr-checks: same step list (i18n generation, malware gate,
    # typecheck, four builds), so the same 20 minute bound.
    timeout-minutes: 20
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Set up pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.34.5

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 26
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Generate i18n artifacts
        run: npm run i18n:gen

      # Post the coverage counts of the gitignored i18n.status.summary.json (written
      # by the i18n:gen step above) to the job summary as the out-of-band audit trail.
      - name: Post i18n coverage summary
        run: node scripts/i18n_coverage_summary.mjs

      # Fast, legible fail BEFORE the long build steps if a committed generated
      # artifact drifted from source (someone changed an overlay or a catalog key but
      # forgot to regenerate). The status registry and its counts summary are both
      # gitignored; the diff covers the committed line-item locale slices AND the
      # generated TranslationKey union. The test suite also enforces this, this just
      # surfaces it sooner.
      - name: Verify committed i18n artifacts are fresh
        run: git diff --exit-code -- src/ui/i18n.resolved.generated src/admin/i18n.resolved.generated src/ui/i18n.catalog/translation_keys.generated.ts

      # Release-tier malicious-code scan (see pr-checks for the rationale).
      # --gate fails only on a HIGH-severity signature surviving the path-aware
      # priors; the full agent-judged pass is the `release-malware-audit` skill.
      - name: Malicious-code gate
        run: npm run security:gate

      # Same tsc incremental cache as pr-checks (#2758). release-checks is not
      # matrixed, so no matrix.shard gate (Phase 4: checks run once, not on
      # release-gate shard 1).
      - name: Cache tsc incremental buildinfo
        uses: actions/cache@v4
        with:
          path: node_modules/.cache/tsc
          key: tsc-buildinfo-${{ runner.os }}-${{ hashFiles('pnpm-lock.yaml', 'tsconfig.json') }}-${{ github.sha }}
          restore-keys: |
            tsc-buildinfo-${{ runner.os }}-${{ hashFiles('pnpm-lock.yaml', 'tsconfig.json') }}-

      # Same turbo multi-task run gate.mjs uses for wall-clock overlap across
      # these four independent pure steps (scripts/lib/gate_steps.mjs), so CI and
      # the local gate share one cache-aware idiom instead of drifting apart.
      # Same reason as the PR-tier job: the bot bundles from this tree and ships
      # in the same image, so a broken bot bundle fails here, not on the host.
      - name: Typecheck + env/server/bot builds
        run: npx turbo run check:types build:env build:server build:bot --ui=stream

      - name: Build client
        run: npm run wiki:content && npm run build:bundle

      # The client build just regenerated all three committed build manifests
      # (wiki:content writes the guide content, build:bundle's pregen writes
      # the SFX and media manifests), so trackedness plus the diff prove the
      # committed copies against this tree's own sources. The selective planner treats
      # exactly these paths as inert-but-fed-to-related ONLY on the strength
      # of this step (GENERATED_MANIFEST_ARTIFACT_FILES in
      # scripts/lib/gate_select_plan.mjs; tests/ci_workflow.test.ts welds the
      # classifier list, this argv, and the local gate list to each other).
      - name: Manifest freshness (committed build manifests)
        run: git ls-files --error-unmatch -- src/game/sfx_manifest.generated.ts src/guide/content.generated.ts src/render/assets/manifest.generated.ts public/audio/sfx/runtime-pack.json scripts/sfx/sfx_gain_ceiling.generated.json && git diff --exit-code -- src/game/sfx_manifest.generated.ts src/guide/content.generated.ts src/render/assets/manifest.generated.ts public/audio/sfx/runtime-pack.json scripts/sfx/sfx_gain_ceiling.generated.json
