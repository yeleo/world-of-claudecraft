name: Desktop publish

# Builds and publishes the website-channel desktop artifacts to the update
# host (Cloudflare R2 behind https://updates.worldofclaudecraft.com/desktop/):
#   - Linux: AppImage + deb (x64 + arm64), SHA256SUMS-linux, and the two
#     per-arch electron-updater feed files. No signing required.
#   - macOS: signed + notarized universal dmg + zip + blockmap, SHA256SUMS-mac,
#     and latest-mac.yml. Requires the Apple signing secrets; the job refuses
#     to publish an ad-hoc build.
#   - Windows: the Azure-Key-Vault-signed per-arch NSIS installers (one
#     win-x64.exe and one win-arm64.exe, build.nsis.buildUniversalInstaller:
#     false, issue 2013) + their blockmaps + the per-arch zips,
#     SHA256SUMS-windows, and latest.yml (both installers list in the SAME
#     feed file; Windows update-info filenames carry no arch suffix). Requires
#     the Azure Key Vault signing secrets; the job refuses to publish an
#     unsigned build.
# The full runbook, including the one-time R2, Apple, and Azure provisioning
# these jobs depend on, is docs/desktop-release.md ("Publishing from CI").
#
# Triggers:
#   - a release tag (v<version>) pushed to a commit that is on main; the tag
#     must match package.json "version". The download page derives its version
#     from package.json at build time (src/game/desktop_download.ts via the
#     __APP_VERSION__ define), so that one check keeps the download page, the
#     feed, and the artifacts in lockstep.
#   - manual dispatch, which is a DRY RUN by default: it builds, signs,
#     verifies, and checksums exactly like a release, then attaches the
#     artifacts to the workflow run for inspection instead of uploading.
#     Tick "publish" to really upload (the backfill path). Each platform job
#     still verifies the artifact names carry package.json "version"; only
#     the tag and main-ancestry checks are skipped.
#
# The platform jobs are independent: a mac signing failure never blocks the
# Linux publish and vice versa. Within each job, versioned artifacts upload
# before the feed files, so an installed app is never offered an update whose
# file is not yet downloadable.

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      publish:
        description: 'Upload to the live update host (leave unchecked for a build-only dry run)'
        type: boolean
        default: false

permissions:
  contents: write

concurrency:
  group: desktop-publish
  cancel-in-progress: false

jobs:
  verify:
    name: Verify release guards
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Check out repository
        uses: actions/checkout@v6
        with:
          # Full history so the main-ancestry guard below can walk it.
          fetch-depth: 0

      # A tag alone must not be able to publish code that never landed on release/china
      # (the contract is: merge to release/china, then tag the merge).
      - name: Verify the tagged commit is on release/china (tag pushes only)
        if: github.event_name == 'push'
        run: |
          git fetch --no-tags origin release/china
          if ! git merge-base --is-ancestor "$GITHUB_SHA" origin/release/china; then
            echo "Tag ${GITHUB_REF_NAME} points at ${GITHUB_SHA}, which is not on release/china. Refusing to publish." >&2
            exit 1
          fi

      # Artifact names, the updater feed, and the download-page links all key
      # off package.json "version"; the download page derives it at build time
      # (src/game/desktop_download.ts via the __APP_VERSION__ define, pinned
      # equal to package.json by tests/desktop_download.test.ts), so there is
      # no second constant to drift. Fail fast on a mismatched tag before the
      # long builds. Tag pushes do not run the vitest pin, so the greps below
      # keep the derive mechanism itself honest at publish time: each anchors
      # on the load-bearing expression (the assignment reading the define, and
      # the define entry itself), never any version value, so a mention in a
      # comment or a type-only declare cannot satisfy it. The exact patterns
      # are extracted and executed both ways by tests/desktop_publish_guard.test.ts.
      - name: Verify version lockstep
        run: |
          VERSION="$(node -p "require('./package.json').version")"
          echo "package.json version: ${VERSION}"
          if [ "${GITHUB_EVENT_NAME}" = "push" ] && [ "${GITHUB_REF_NAME}" != "v${VERSION}" ] && [ "${GITHUB_REF_NAME}" != "v${VERSION}-cn" ]; then
            echo "Tag ${GITHUB_REF_NAME} does not match package.json version v${VERSION}." >&2
            exit 1
          fi
          if ! grep -qF "DESKTOP_VERSION = typeof __APP_VERSION__" src/game/desktop_download.ts; then
            echo "src/game/desktop_download.ts no longer derives its version from the __APP_VERSION__ define; the download page would go stale. See docs/desktop-release.md." >&2
            exit 1
          fi
          if ! grep -qF "__APP_VERSION__: JSON.stringify(appVersion)" vite.config.ts; then
            echo "vite.config.ts no longer defines __APP_VERSION__; the download-page version would not be baked at build time. See docs/desktop-release.md." >&2
            exit 1
          fi

  linux:
    name: Build and publish Linux artifacts
    needs: verify
    runs-on: ubuntu-latest
    timeout-minutes: 60

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Set up pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.34.5

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      # electron-builder downloads Electron itself plus its packaging tools
      # into this cache directory on every build; a cold run redownloads tens
      # to hundreds of MB before packaging even starts. Keyed on the
      # lockfile since the Electron/electron-builder versions live in
      # package.json devDependencies, which the lockfile fingerprints.
      - name: Cache electron-builder downloads
        uses: actions/cache@v4
        with:
          path: |
            ~/.cache/electron
            ~/.cache/electron-builder
          key: electron-builder-${{ runner.os }}-${{ hashFiles('pnpm-lock.yaml') }}
          restore-keys: |
            electron-builder-${{ runner.os }}-

      # Full website-channel build: vite client bundle baked for production,
      # then electron-builder producing the Linux arch matrix from
      # package.json "build" (AppImage + deb, x64 + arm64) plus the per-arch
      # feed files latest-linux.yml (x64) and latest-linux-arm64.yml (arm64).
      # The script itself passes --publish never; uploading is the explicit,
      # ordered step below.
      - name: Build Linux desktop artifacts
        run: npm run electron:build

      # Checksums so Linux users can validate their download
      # (docs/desktop-release.md, "Linux"). Bare filenames, so the file
      # verifies with "sha256sum -c" from the download directory. Named
      # per-platform because the mac job publishes its own checksum file to
      # the same prefix.
      - name: Generate SHA256SUMS-linux
        run: |
          cd release
          sha256sum *.AppImage *.deb > SHA256SUMS-linux
          cat SHA256SUMS-linux

      # Pin the exact expected artifact names instead of globbing: the x64
      # AppImage arch token is "x86_64" while the x64 deb is "amd64" (both
      # electron-builder conventions), and a silent rename here would strand
      # the download page and the update feed. Runs on dry runs too, so a
      # rehearsal proves the same contract a release relies on.
      - name: Verify expected artifacts
        run: |
          VERSION="$(node -p "require('./package.json').version")"
          FILES=(
            "world-of-claudecraft-${VERSION}-linux-x86_64.AppImage"
            "world-of-claudecraft-${VERSION}-linux-arm64.AppImage"
            "world-of-claudecraft-${VERSION}-linux-amd64.deb"
            "world-of-claudecraft-${VERSION}-linux-arm64.deb"
            "SHA256SUMS-linux"
            "latest-linux.yml"
            "latest-linux-arm64.yml"
          )
          for file in "${FILES[@]}"; do
            if [ ! -f "release/${file}" ]; then
              echo "Expected artifact release/${file} was not produced; refusing to publish a partial release." >&2
              ls release >&2
              exit 1
            fi
          done

      - name: Attach artifacts to the run (dry run only)
        if: github.event_name == 'workflow_dispatch' && !inputs.publish
        uses: actions/upload-artifact@v4
        with:
          name: desktop-linux
          retention-days: 7
          path: |
            release/*.AppImage
            release/*.deb
            release/SHA256SUMS-linux
            release/latest-linux*.yml

      # Versioned artifacts are immutable (cache for a year); the checksum and
      # feed files live at stable keys that change each release, so they stay
      # near-uncached. Feed files upload LAST.
      - name: Upload Linux artifacts to GitHub Release
        if: github.event_name == 'push' || inputs.publish
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          TAG="${GITHUB_REF_NAME}"
          if [ "$GITHUB_EVENT_NAME" != "push" ]; then
            TAG="v$(node -p "require('./package.json').version")-cn"
          fi
          echo "Ensuring GitHub Release ${TAG} exists..."
          gh release view "${TAG}" || gh release create "${TAG}" --title "World of ClaudeCraft ${TAG}" --notes "Automated release build."
          echo "Uploading Linux artifacts to GitHub Release ${TAG}..."
          gh release upload "${TAG}" release/*.AppImage release/*.deb release/SHA256SUMS-linux release/latest-linux*.yml --clobber

  mac:
    name: Build and publish macOS artifacts
    # 暂时停止 macOS 打包发布
    if: false
    needs: verify
    runs-on: macos-latest
    timeout-minutes: 90

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      # HARD DEPENDENCY (docs/desktop-release.md, "macOS"): auto-update only
      # applies to a Developer-ID-signed AND notarized build, so this job
      # refuses to run unsigned rather than publish an ad-hoc build. Checked
      # before the long install/build.
      - name: Check Apple signing secrets
        id: check_apple_sign
        env:
          CSC_LINK: ${{ secrets.CSC_LINK }}
          CSC_KEY_PASSWORD: ${{ secrets.CSC_KEY_PASSWORD }}
        run: |
          if [ -n "$CSC_LINK" ] && [ -n "$CSC_KEY_PASSWORD" ]; then
            echo "has_apple_sign=true" >> "$GITHUB_OUTPUT"
          else
            echo "has_apple_sign=false" >> "$GITHUB_OUTPUT"
            echo "Apple signing secrets not configured. Building ad-hoc signed package for testing."
          fi
      - name: Set up pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.34.5

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      # electron-builder downloads Electron itself plus its packaging tools
      # into this cache directory on every build; a cold run redownloads tens
      # to hundreds of MB before packaging even starts. Keyed on the
      # lockfile since the Electron/electron-builder versions live in
      # package.json devDependencies, which the lockfile fingerprints.
      - name: Cache electron-builder downloads
        uses: actions/cache@v4
        with:
          path: |
            ~/Library/Caches/electron
            ~/Library/Caches/electron-builder
          key: electron-builder-${{ runner.os }}-${{ hashFiles('pnpm-lock.yaml') }}
          restore-keys: |
            electron-builder-${{ runner.os }}-

      # electron-builder 26.15.7 imports CSC_LINK into a keychain of its own and
      # then runs `security set-key-partition-list -k` with the CERTIFICATE
      # password instead of the keychain password. The macOS 26.6 runner image
      # (20260831) rejects that call ("SecKeychainUnlock: The user name or
      # passphrase you entered is not correct") where 26.5 let it through;
      # electron-builder fixed it in 26.16.1 ("use keychain password for
      # set-key-partition-list"). Until that bump lands (it moves pnpm-lock.yaml,
      # which re-mints every GLB seal), this step owns the keychain: it imports
      # the .p12 with the right passwords, adds electron-builder's bundled Apple
      # root keychain to the search list exactly as its createKeychain would, and
      # hands electron-builder the result through CSC_KEYCHAIN plus the identity
      # name through CSC_NAME (scripts/electron-build.mjs treats CSC_NAME as the
      # real-signing signal). The build step deliberately does NOT receive
      # CSC_LINK: with it set, electron-builder would create its own keychain
      # again and fail the same way.
      - name: Import the signing certificate into a temporary keychain
        if: steps.check_apple_sign.outputs.has_apple_sign == 'true'
        env:
          CSC_LINK: ${{ secrets.CSC_LINK }}
          CSC_KEY_PASSWORD: ${{ secrets.CSC_KEY_PASSWORD }}
        run: |
          set -euo pipefail
          keychain="$RUNNER_TEMP/woc-signing.keychain-db"
          keychain_password="$(openssl rand -base64 24)"
          cert="$RUNNER_TEMP/csc.p12"
          # CSC_LINK is the base64 .p12 (docs/desktop-release.md, "Publishing from
          # CI"); an optional data: URI prefix is stripped, and the path/URL forms
          # electron-builder also accepts are refused because this step does not fetch.
          case "$CSC_LINK" in
            http://*|https://*|file:*|/*)
              echo "CSC_LINK must be the base64 .p12 for the keychain import step" >&2
              exit 1 ;;
          esac
          printf '%s' "${CSC_LINK#data:*;base64,}" | base64 --decode > "$cert"
          security create-keychain -p "$keychain_password" "$keychain"
          security set-keychain-settings -lut 21600 "$keychain"
          security unlock-keychain -p "$keychain_password" "$keychain"
          security import "$cert" -k "$keychain" -P "$CSC_KEY_PASSWORD" \
            -T /usr/bin/codesign -T /usr/bin/productbuild -T /usr/bin/security
          security set-key-partition-list -S apple-tool:,apple: -s -k "$keychain_password" "$keychain"
          rm -f "$cert"
          root_certs="$(find node_modules -name root_certs.keychain -path '*/app-builder-lib/certs/*' -print -quit)"
          if [ -z "$root_certs" ]; then
            echo "app-builder-lib's bundled root_certs.keychain was not found under node_modules" >&2
            exit 1
          fi
          cp "$root_certs" "$RUNNER_TEMP/electron-builder-root-certs.keychain"
          existing="$(security list-keychains -d user | tr -d '" ')"
          # shellcheck disable=SC2086
          security list-keychains -d user -s "$keychain" "$RUNNER_TEMP/electron-builder-root-certs.keychain" $existing
          # electron-builder wants the name WITHOUT the "Developer ID Application:"
          # prefix (it refuses the prefixed form and picks the certificate type itself).
          identity="$(security find-identity -v -p codesigning "$keychain" \
            | sed -n 's/.*"Developer ID Application: \([^"]*\)".*/\1/p' | head -n 1)"
          if [ -z "$identity" ]; then
            echo "No valid Developer ID Application identity in the imported certificate" >&2
            security find-identity -p codesigning "$keychain" >&2 || true
            exit 1
          fi
          echo "CSC_KEYCHAIN=$keychain" >> "$GITHUB_ENV"
          echo "CSC_NAME=$identity" >> "$GITHUB_ENV"

      # The App Store Connect API key must be a file on disk, so the .p8 secret
      # content is written to the runner temp dir and APPLE_API_KEY points at it.
      # Signing (CSC_KEYCHAIN and CSC_NAME from the step above) and notarization
      # then activate automatically in scripts/electron-build.mjs / electron-builder.
      - name: Build signed and notarized macOS desktop artifacts
        env:
          APPLE_API_KEY_P8: ${{ secrets.APPLE_API_KEY_P8 }}
          APPLE_API_KEY_ID: ${{ secrets.APPLE_API_KEY_ID }}
          APPLE_API_ISSUER: ${{ secrets.APPLE_API_ISSUER }}
        run: |
          printf '%s' "$APPLE_API_KEY_P8" > "$RUNNER_TEMP/AuthKey.p8"
          export APPLE_API_KEY="$RUNNER_TEMP/AuthKey.p8"
          npm run electron:build

      # Mirrors the manual runbook verification: a bad signature or a build
      # that skipped notarization must fail here, never publish.
      - name: Verify code signature and notarization
        if: steps.check_apple_sign.outputs.has_apple_sign == 'true'
        run: |
          codesign --verify --deep --strict "release/mac-universal/World of ClaudeCraft.app"
          spctl -a -t exec -vv "release/mac-universal/World of ClaudeCraft.app"

      - name: Generate SHA256SUMS-mac
        run: |
          cd release
          shasum -a 256 *.dmg *.zip > SHA256SUMS-mac
          cat SHA256SUMS-mac

      # Pinned filenames, same contract as the Linux job; runs on dry runs too.
      - name: Verify expected artifacts
        run: |
          VERSION="$(node -p "require('./package.json').version")"
          FILES=(
            "world-of-claudecraft-${VERSION}-mac-universal.dmg"
            "world-of-claudecraft-${VERSION}-mac-universal.zip"
            "world-of-claudecraft-${VERSION}-mac-universal.zip.blockmap"
            "SHA256SUMS-mac"
            "latest-mac.yml"
          )
          for file in "${FILES[@]}"; do
            if [ ! -f "release/${file}" ]; then
              echo "Expected artifact release/${file} was not produced; refusing to publish a partial release." >&2
              ls release >&2
              exit 1
            fi
          done

      - name: Attach artifacts to the run (dry run only)
        if: github.event_name == 'workflow_dispatch' && !inputs.publish
        uses: actions/upload-artifact@v4
        with:
          name: desktop-mac
          retention-days: 7
          path: |
            release/*.dmg
            release/*.zip
            release/*.zip.blockmap
            release/SHA256SUMS-mac
            release/latest-mac.yml

      # Same rules as the Linux upload: immutable caching for the versioned
      # artifacts, near-uncached checksum + feed, and the feed file
      # (latest-mac.yml) uploads LAST.
      - name: Upload macOS artifacts to GitHub Release
        if: github.event_name == 'push' || inputs.publish
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          TAG="${GITHUB_REF_NAME}"
          if [ "$GITHUB_EVENT_NAME" != "push" ]; then
            TAG="v$(node -p "require('./package.json').version")-cn"
          fi
          echo "Ensuring GitHub Release ${TAG} exists..."
          gh release view "${TAG}" || gh release create "${TAG}" --title "World of ClaudeCraft ${TAG}" --notes "Automated release build."
          echo "Uploading macOS artifacts to GitHub Release ${TAG}..."
          gh release upload "${TAG}" release/*.dmg release/*.zip release/*.blockmap release/SHA256SUMS-mac release/latest-mac.yml --clobber

  windows:
    name: Build and publish Windows artifacts
    needs: verify
    runs-on: windows-latest
    timeout-minutes: 90

    # The runner's default shell is pwsh; the R2 upload and verification steps
    # keep the same bash + put() shape as the linux/mac jobs, so default the
    # whole job to bash (present on the windows runners). The one step that
    # genuinely needs PowerShell (Get-AuthenticodeSignature) overrides back.
    defaults:
      run:
        shell: bash

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      # HARD DEPENDENCY: an unsigned installer trips SmartScreen hard and
      # taints publisher reputation, so this job refuses to run unsigned
      # rather than publish an unsigned build. These five secrets activate
      # the Azure Key Vault sign hook (scripts/electron-win-sign.mjs) inside
      # scripts/electron-builder-config.mjs; checked before the long
      # install/build. The timestamp/digest secrets are optional (the hook
      # defaults them to DigiCert + sha256).
      - name: Check Azure signing secrets
        id: check_win_sign
        shell: pwsh
        run: |
          if ($env:AZURE_KEY_VAULT_URL -and $env:AZURE_CLIENT_ID) {
            Write-Output "has_win_sign=true" >> $env:GITHUB_OUTPUT
          } else {
            Write-Output "has_win_sign=false" >> $env:GITHUB_OUTPUT
            Write-Output "Azure Key Vault secrets not configured. Building standard unsigned package."
          }
      - name: Set up pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.34.5

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      # electron-builder downloads Electron itself plus its packaging
      # helper binaries (winCodeSign, nsis, etc) into these two folders on
      # first use; without caching, every publish run re-downloads the
      # same files even though the versions are pinned by pnpm-lock.yaml
      # and rarely change between runs.
      - name: Cache electron and electron-builder downloads
        uses: actions/cache@v4
        with:
          path: |
            ${{ env.LOCALAPPDATA }}\electron\Cache
            ${{ env.LOCALAPPDATA }}\electron-builder\Cache
          key: ${{ runner.os }}-electron-builder-${{ hashFiles('pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-electron-builder-

      # dotnet tool install --global resolves and restores AzureSignTool
      # through the local NuGet global-packages folder; caching it avoids
      # re-downloading the tool and its dependencies on every run. The
      # install step below pins no explicit version, so there is no
      # version to key on: the key is a fixed string.
      - name: Cache NuGet packages
        uses: actions/cache@v4
        with:
          path: ~/.nuget/packages
          key: ${{ runner.os }}-nuget-azuresigntool

      # The sign hook shells out to the AzureSignTool dotnet global tool; the
      # runner's dotnet tools directory is already on PATH.
      - name: Install AzureSignTool
        if: steps.check_win_sign.outputs.has_win_sign == 'true'
        run: dotnet tool install --global AzureSignTool

      # Full website-channel build: the Windows target matrix from
      # package.json "build" is nsis + zip for x64 + arm64, and
      # nsis.buildUniversalInstaller: false makes electron-builder emit a
      # SEPARATE installer per arch (win-x64.exe, win-arm64.exe) instead of
      # one dual-arch exe, both listed in the same latest.yml (issue 2013).
      # Every signable file (each installer, the app exe inside the per-arch
      # zips, the uninstaller) is signed by the hook as electron-builder
      # emits it.
      - name: Build signed Windows desktop artifacts
        env:
          AZURE_TENANT_ID: ${{ secrets.AZURE_TENANT_ID }}
          AZURE_CLIENT_ID: ${{ secrets.AZURE_CLIENT_ID }}
          AZURE_CLIENT_SECRET: ${{ secrets.AZURE_CLIENT_SECRET }}
          AZURE_KEY_VAULT_URL: ${{ secrets.AZURE_KEY_VAULT_URL }}
          AZURE_KEY_VAULT_CERTIFICATE: ${{ secrets.AZURE_KEY_VAULT_CERTIFICATE }}
          CODE_SIGN_TIMESTAMP_URL: ${{ secrets.CODE_SIGN_TIMESTAMP_URL }}
          CODE_SIGN_FILE_DIGEST: ${{ secrets.CODE_SIGN_FILE_DIGEST }}
          CODE_SIGN_TIMESTAMP_DIGEST: ${{ secrets.CODE_SIGN_TIMESTAMP_DIGEST }}
        run: npm run electron:build

      # Mirrors the mac job's signature gate: a build whose installer is not
      # Authenticode-Valid must fail here, never publish. The installer name
      # comes from the feed, not a pinned literal (docs/desktop-release.md:
      # verify the emitted name from latest.yml rather than assume it).
      - name: Verify Authenticode signature
        if: steps.check_win_sign.outputs.has_win_sign == 'true'
        shell: pwsh
        run: |
          $feed = Get-Content release/latest.yml -Raw
          $names = [regex]::Matches($feed, '(?m)^\s*(?:-\s*)?(?:url|path):\s*"?([^"\s]+)"?') |
            ForEach-Object { $_.Groups[1].Value } | Sort-Object -Unique
          $exes = @($names | Where-Object { $_ -like '*.exe' })
          if ($exes.Count -eq 0) {
            Write-Error 'latest.yml lists no installer to verify; refusing to publish.'
            exit 1
          }
          foreach ($name in $exes) {
            $sig = Get-AuthenticodeSignature -FilePath "release/$name"
            if ($sig.Status -ne 'Valid') {
              Write-Error "release/$name Authenticode status is $($sig.Status), not Valid; refusing to publish an unsigned build."
              exit 1
            }
            Write-Output "release/$name signed by $($sig.SignerCertificate.Subject) (status: $($sig.Status))"
          }

      # Checksums so Windows users can validate their download; bare filenames
      # for "sha256sum -c", named per-platform like the other jobs.
      - name: Generate SHA256SUMS-windows
        run: |
          cd release
          sha256sum *.exe *.zip > SHA256SUMS-windows
          cat SHA256SUMS-windows

      # Unlike the linux/mac jobs this does NOT pin literal filenames: the
      # per-arch installer names are defined by what electron-builder emitted
      # into latest.yml (docs/desktop-release.md), so the feed is the source
      # of truth and every file it references (both arches) must exist, carry
      # this release's version, and (for each installer) have its blockmap. A
      # dev*.yml misbake fails here instead of publishing. Runs on dry runs
      # too, so a rehearsal proves the same contract a release relies on.
      - name: Verify expected artifacts
        run: |
          VERSION="$(node -p "require('./package.json').version")"
          if [ ! -f release/latest.yml ]; then
            echo "release/latest.yml was not produced (a dev-origin misbake emits dev.yml instead); refusing to publish." >&2
            ls release >&2
            exit 1
          fi
          if ls release/dev*.yml >/dev/null 2>&1; then
            echo "dev-channel feed files found next to latest.yml; the build was not baked for production. Refusing to publish." >&2
            ls release >&2
            exit 1
          fi
          FEED_FILES="$(node -e '
            const text = require("fs").readFileSync("release/latest.yml", "utf8");
            const names = new Set();
            for (const m of text.matchAll(/^\s*(?:-\s*)?(?:url|path):\s*"?([^"\s]+)"?\s*$/gm)) names.add(m[1]);
            console.log([...names].join("\n"));
          ')"
          if [ -z "$FEED_FILES" ]; then
            echo "latest.yml lists no artifacts; refusing to publish." >&2
            exit 1
          fi
          FOUND_EXE=0
          while IFS= read -r file; do
            if [ ! -f "release/${file}" ]; then
              echo "latest.yml references ${file}, which was not produced; refusing to publish a partial release." >&2
              ls release >&2
              exit 1
            fi
            case "$file" in
              *"${VERSION}"*) ;;
              *)
                echo "latest.yml references ${file}, which does not carry version ${VERSION}; stale feed or half-bumped release." >&2
                exit 1
                ;;
            esac
            case "$file" in
              *.exe)
                FOUND_EXE=1
                if [ ! -f "release/${file}.blockmap" ]; then
                  echo "Installer blockmap release/${file}.blockmap was not produced; differential updates would break." >&2
                  exit 1
                fi
                ;;
            esac
          done <<< "$FEED_FILES"
          if [ "$FOUND_EXE" != "1" ]; then
            echo "latest.yml lists no NSIS installer; refusing to publish." >&2
            exit 1
          fi
          if [ ! -f release/SHA256SUMS-windows ]; then
            echo "release/SHA256SUMS-windows was not produced." >&2
            exit 1
          fi

      - name: Attach artifacts to the run (dry run only)
        if: github.event_name == 'workflow_dispatch' && !inputs.publish
        uses: actions/upload-artifact@v4
        with:
          name: desktop-windows
          retention-days: 7
          path: |
            release/*.exe
            release/*.exe.blockmap
            release/*.zip
            release/SHA256SUMS-windows
            release/latest.yml

      # Same rules as the other uploads: immutable caching for the versioned
      # artifacts, near-uncached checksum + feed, and the feed file
      # (latest.yml) uploads LAST. Versioned names come from the feed plus the
      # per-arch zips, matching the verification above.
      - name: Upload Windows artifacts to GitHub Release
        if: github.event_name == 'push' || inputs.publish
        shell: bash
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          TAG="${GITHUB_REF_NAME}"
          if [ "$GITHUB_EVENT_NAME" != "push" ]; then
            TAG="v$(node -p "require('./package.json').version")-cn"
          fi
          echo "Ensuring GitHub Release ${TAG} exists..."
          gh release view "${TAG}" || gh release create "${TAG}" --title "World of ClaudeCraft ${TAG}" --notes "Automated release build."
          echo "Uploading Windows artifacts to GitHub Release ${TAG}..."
          gh release upload "${TAG}" release/*.exe release/*.zip release/*.blockmap release/SHA256SUMS-windows release/latest.yml --clobber

  android:
    name: Build and publish Android APK
    needs: verify
    runs-on: ubuntu-latest
    timeout-minutes: 60

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Set up Java 21
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '21'

      - name: Set up pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.34.5

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Sync web assets to native Android project
        run: npm run native:sync
        env:
          VITE_API_ORIGIN: https://worldofclaudecraft.aoruantech.com

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v3

      - name: Build Android APK
        run: |
          cd android
          chmod +x ./gradlew
          ./gradlew assemblePlayDebug

      - name: Collect and rename APK
        run: |
          VERSION="$(node -p "require('./package.json').version")"
          mkdir -p release
          APK_FILE="$(find android/app/build/outputs/apk -name "*debug.apk" -type f | head -n 1)"
          if [ -n "$APK_FILE" ]; then
            cp "$APK_FILE" "release/world-of-claudecraft-${VERSION}-android.apk"
            echo "Successfully generated release/world-of-claudecraft-${VERSION}-android.apk"
          else
            echo "Error: APK not found under android/app/build/outputs/apk" >&2
            find android/app/build/outputs >&2 || true
            exit 1
          fi

      - name: Upload Android APK to GitHub Release
        if: github.event_name == 'push' || inputs.publish
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          TAG="${GITHUB_REF_NAME}"
          if [ "$GITHUB_EVENT_NAME" != "push" ]; then
            TAG="v$(node -p "require('./package.json').version")-cn"
          fi
          echo "Ensuring GitHub Release ${TAG} exists..."
          gh release view "${TAG}" || gh release create "${TAG}" --title "World of ClaudeCraft ${TAG}" --notes "Automated release build."
          echo "Uploading Android APK to GitHub Release ${TAG}..."
          gh release upload "${TAG}" release/*-android.apk --clobber
